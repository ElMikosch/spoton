---
phase: 54-auth-health-dashboard
plan: 05
type: execute
wave: 3
depends_on: ["54-01", "54-02", "54-03", "54-04"]
files_modified:
  - CHANGELOG.md
  - Plugins/SpotOn/install.xml
  - repo.xml
autonomous: false
requirements: ["D-06", "D-07", "D-08", "D-10", "D-11"]
must_haves:
  truths:
    - "v2-legacy branch exists with a frozen repo.xml pointing at the v2.3.18 GitHub Release ZIP (correct SHA af5f2dd0, correct URL)"
    - "v3.0-auth is merged into main with a merge commit preserving full history"
    - "install.xml and repo.xml both report version 3.0.0 on main"
    - "CHANGELOG.md has a v3.0.0 entry documenting the PKCE migration"
    - "PKCE migration E2E verified for happy path and 3 edge cases (D-10)"
    - "main is NOT pushed to origin until explicit release approval (repo.xml still shows v2.3.18 URL+SHA until release)"
  artifacts:
    - "v2-legacy git branch with correct repo.xml (SHA af5f2dd0, v2.3.18 URL)"
    - "Merge commit on main from v3.0-auth"
    - "Plugins/SpotOn/install.xml with version 3.0.0"
    - "repo.xml with version 3.0.0 (URL+SHA placeholder -- updated at release time by spoton-release skill)"
    - "CHANGELOG.md with ## [3.0.0] entry"
  key_links:
    - "v2-legacy repo.xml -> GitHub Release v2.3.18 ZIP asset (SHA af5f2dd0)"
    - "main install.xml version -> repo.xml version (must match 3.0.0)"
---

<objective>
Create the v2-legacy fallback branch (D-06), merge v3.0-auth into main (D-07), bump version to 3.0.0 (D-08), and verify the PKCE migration E2E (D-10). D-09 (GitHub Pages) is already live from Phase 53. D-11 confirms audio-key cohort is NOT separately tested in E2E.

This is the final plan before the v3.0.0 release. The plan prepares all release artifacts but does NOT create a git tag, GitHub Release, or push main to origin -- that requires explicit user approval per project rules (Kein Auto-Release).

CRITICAL: v2-legacy must branch from main HEAD (not from v2.3.18 tag). The tag's repo.xml incorrectly points at the v2.3.17 ZIP (SHA 0a6849ca, URL v2.3.17). Main HEAD has the correct repo.xml (SHA af5f2dd0, URL v2.3.18). This was a verified factual error in the original plan.

Purpose: Complete the v3.0 auth overhaul by merging all work into main, providing a v2-legacy fallback, and verifying the migration path end-to-end.

Output: v2-legacy branch, merge commit on main, version 3.0.0, CHANGELOG entry, E2E verification.

Addresses review concerns: v2-legacy from main HEAD (not tag), merge conflict contingency, clean-tree check, main push-gate, repo.xml URL+SHA awareness.
</objective>

## Artifacts this phase produces

- Git branch `v2-legacy` from main HEAD (correct repo.xml)
- Merge commit on `main` from `v3.0-auth`
- `Plugins/SpotOn/install.xml` version 3.0.0
- `repo.xml` version 3.0.0 (URL+SHA are placeholders until release -- spoton-release skill updates them at tag time)
- `CHANGELOG.md` section `## [3.0.0]`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-auth-health-dashboard/54-CONTEXT.md
@.planning/phases/54-auth-health-dashboard/54-RESEARCH.md
@Plugins/SpotOn/install.xml
@repo.xml
@CHANGELOG.md
@.claude/skills/spoton-release/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v2-legacy branch + prepare CHANGELOG</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (current state -- existing Keep a Changelog format, latest entry structure)
    - repo.xml (verify on current main HEAD that it shows SHA af5f2dd0 and URL v2.3.18 -- NOT the tag version)
    - Plugins/SpotOn/install.xml (line 19 -- current version 2.3.18)
    - .claude/skills/spoton-release/SKILL.md (version-bump locations, CI/SHA workflow, Hard Rule 1 on version autonomy -- note: version 3.0.0 is an explicit user decision D-08, not autonomous)
  </read_first>
  <action>
    Step 0: CLEAN TREE CHECK (addresses review concern about dirty working tree).
    Run: git status --porcelain
    If working tree has uncommitted changes, stash or commit them BEFORE proceeding.
    The merge in Task 2 requires a clean working tree.

    Step 1: Create v2-legacy branch from MAIN HEAD (NOT from v2.3.18 tag).
    CRITICAL: The tag v2.3.18 has an incorrect repo.xml that points at the v2.3.17 ZIP
    (SHA 0a6849ca, URL ...v2.3.17/SpotOn-v2.3.17.zip). The SHA-fix commit was made AFTER
    the tag, so main HEAD has the correct repo.xml (SHA af5f2dd0, URL ...v2.3.18/SpotOn-v2.3.18.zip).
    Run: git checkout main
    Run: git checkout -b v2-legacy
    Run: git push -u origin v2-legacy
    Run: git checkout v3.0-auth (return to working branch)

    Verify immediately after branch creation:
    Run: git show v2-legacy:repo.xml | grep -E 'url|sha'
    Expected: sha="af5f2dd0ba21c7d8f5cc2673338edd98aa2ee97d" and url="...v2.3.18/SpotOn-v2.3.18.zip"
    If the SHA shows 0a6849ca or the URL shows v2.3.17, STOP -- wrong base was used.

    The v2-legacy branch is a frozen snapshot. D-06 explicitly says "Kein Maintenance geplant,
    reine Fallback-Option."

    Step 2: Write the v3.0.0 CHANGELOG entry on v3.0-auth.
    Add a new ## [3.0.0] section at the top of CHANGELOG.md (above the existing latest
    entry), following the established Keep a Changelog format. The entry should document:

    ### Changed
    - Authentication: PKCE OAuth replaces ZeroConf/Keymaster as the primary auth mechanism
    - Token management: PKCE-native TokenManager with refresh token rotation
    - Connect: Stored credentials derived from PKCE tokens (no ZeroConf needed for Connect)
    - Auth Health Dashboard: Per-account status indicators on the Settings page

    ### Added
    - PKCE OAuth flow via GitHub Pages static relay (one-click browser auth)
    - sp_dc cookie support for Made For You playlists (Pathfinder integration)
    - Credential derivation: automatic conversion of PKCE tokens to Connect credentials
    - Migration detection and guided re-auth flow for existing v2.x accounts
    - Auth Health Dashboard showing PKCE, sp_dc, Connect, migration, and audio-key status
    - Client-ID PKCE setup wizard in Settings

    ### Removed
    - Keymaster token service (hm://keymaster/token/authenticated) -- all code paths removed
    - ZeroConf as auth mechanism (retained only for guest LAN Connect discovery)
    - --get-token binary mode (replaced by PKCE refresh in pure Perl)
    - Dual-token flavor system in Client.pm (single PKCE token per account)

    ### Fixed
    - Playlist play-all from folder-level "Play now" (per Plan 04 fix)
    NOTE: If Plan 04 chose Option C (accept behavior), replace "Fixed" with "Changed" and
    document: "Playlist play-all: removed unreachable quantity-threshold code (LMS core
    strips quantity for play commands; behavior matches Spotty-NG)"

    Date field: leave as [Unreleased] -- the spoton-release skill sets the date at tag time.
  </action>
  <verify>
    <automated>git show v2-legacy:repo.xml | grep -c 'v2.3.18'</automated>
  </verify>
  <acceptance_criteria>
    - git branch -a shows v2-legacy (local and remote)
    - git show v2-legacy:repo.xml shows SHA af5f2dd0 and URL containing v2.3.18 (NOT v2.3.17)
    - Current working branch is v3.0-auth (git branch --show-current)
    - CHANGELOG.md has a ## [3.0.0] section with Changed, Added, Removed subsections
    - CHANGELOG.md [3.0.0] section does not set a release date (uses [Unreleased] or no date)
  </acceptance_criteria>
  <done>v2-legacy branch created from main HEAD (correct repo.xml) and pushed; CHANGELOG.md v3.0.0 entry written</done>
</task>

<task type="auto">
  <name>Task 2: Merge v3.0-auth into main + version bump</name>
  <files>Plugins/SpotOn/install.xml, repo.xml</files>
  <read_first>
    - Plugins/SpotOn/install.xml (line 19 -- current version, verify it says 2.3.18 before merge)
    - repo.xml (current version, verify structure for version attribute location)
    - .claude/skills/spoton-release/SKILL.md (version-bump file list: install.xml line 19, repo.xml version attribute)
  </read_first>
  <action>
    Step 1: Ensure all v3.0-auth changes are committed (git status should be clean on v3.0-auth).

    Step 2: Switch to main and merge with a merge commit per D-07:
    Run: git checkout main
    Run: git merge --no-ff v3.0-auth -m "Merge branch 'v3.0-auth' into main -- PKCE Auth Overhaul (v3.0.0)"

    MERGE CONFLICT CONTINGENCY (addresses review concern -- main has 39 commits not in v3.0-auth,
    v3.0-auth has 88 own commits):
    If the merge produces conflicts:
    1. List conflicted files: git diff --name-only --diff-filter=U
    2. For repo.xml: keep the main-HEAD version (v2.3.18 SHA/URL will be overwritten by version
       bump below anyway, but the XML structure must be valid)
    3. For the GitHub Pages relay (if both branches touched it): keep the v3.0-auth version
       (it has the latest PKCE relay implementation)
    4. For any other file: prefer the v3.0-auth side (it has the active development changes)
    5. After resolving: git add resolved files, then git commit (merge commit message as above)
    If conflicts are too complex to resolve automatically, STOP and present the conflict
    list to the user as a blocking checkpoint.

    Step 3: Version bump per D-08 (version 3.0.0 is an explicit user decision, not autonomous):
    - Edit install.xml line 19: change version from 2.3.18 to 3.0.0
    - Edit repo.xml: change the version="2.3.18" attribute to version="3.0.0"
    - NOTE: Do NOT update the URL or SHA in repo.xml -- those still point at v2.3.18 until
      the actual release is built. The spoton-release skill computes SHA from the release ZIP
      at tag time. This means repo.xml temporarily has version="3.0.0" but URL/SHA pointing
      at v2.3.18 -- this is INTENTIONAL and safe because main is NOT pushed to origin until
      release approval (addresses review concern about stale URL+SHA).

    Step 4: Commit the version bump on main:
    Commit message: "chore: bump version to 3.0.0 (D-08)"

    Step 5: Verify the merge state:
    - git show HEAD:Plugins/SpotOn/install.xml should contain version 3.0.0
    - git show HEAD:repo.xml should contain version="3.0.0"
    - git log --oneline --graph -10 should show the merge commit with both parent branches

    CRITICAL: Do NOT push main to origin. Do NOT create a git tag or GitHub Release.
    Main must NOT be pushed until the user gives explicit release approval. If main were
    pushed now, LMS instances would see a "3.0.0 update available" pointing at the old
    v2.3.18 ZIP (addresses review concern about false-update window).
  </action>
  <verify>
    <automated>git show HEAD:Plugins/SpotOn/install.xml | grep -c '3.0.0'</automated>
  </verify>
  <acceptance_criteria>
    - Current branch is main (git branch --show-current)
    - git log --merges -1 --oneline shows the v3.0-auth merge commit
    - git show HEAD:Plugins/SpotOn/install.xml contains version 3.0.0
    - git show HEAD:repo.xml contains version="3.0.0"
    - No git tag v3.0.0 exists (git tag -l v3.0.0 returns empty)
    - No GitHub Release was created
    - main was NOT pushed to origin (git log origin/main..main shows the new commits)
  </acceptance_criteria>
  <done>v3.0-auth merged into main with full commit history; version bumped to 3.0.0; main NOT pushed (awaiting release approval)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify merge state + PKCE Migration E2E</name>
  <files>Plugins/SpotOn/install.xml, repo.xml</files>
  <action>
    Present the following verification checklist to the user. Execute the git verification
    commands and display results. The PKCE E2E and Dashboard visual verification must be
    performed by the user on their dev/test LMS machine.

    Git state verification (run these commands):
    1. git branch -a | grep v2-legacy -- should show the branch
    2. git show v2-legacy:repo.xml | grep -E 'url|sha' -- should show af5f2dd0 and v2.3.18 URL
    3. git log --oneline --graph -5 -- should show the merge commit on main
    4. git show HEAD:Plugins/SpotOn/install.xml | grep version -- should show 3.0.0
    5. git show HEAD:repo.xml | grep version -- should show 3.0.0
    6. git log origin/main..main --oneline -- should show unpushed commits (main NOT pushed yet)
    7. git tag -l v3.0.0 -- should return empty (no tag yet)

    PKCE Migration E2E (user tests on dev/test LMS per D-10):
    Scenario 1 -- v2.x Account: PKCE-Auth via Settings, Browse works after auth
    Scenario 2 -- Second account parallel migration
    Scenario 3 -- Already migrated account (no double flow)
    Scenario 4 -- Aborted auth flow (tab closed, Settings still works, re-attempt works)

    Auth Health Dashboard visual check (user on dev/test LMS):
    Auth Health section visible and collapsible, 5 indicators per account, no raw secrets visible.

    D-11: Audio-key cohort is NOT separately tested (dev account is unaffected cohort).

    RELEASE GATE: Remind user that main is NOT pushed to origin. When ready to release:
    1. Push main: git push origin main
    2. Follow spoton-release skill for tag + GitHub Release + repo.xml SHA update
    Until then, LMS community sees v2.3.18 -- no false update window.

    Wait for user to type "approved" or describe issues.
  </action>
  <verify>
    <automated>git show HEAD:Plugins/SpotOn/install.xml | grep -c '3.0.0'</automated>
  </verify>
  <done>User has verified merge state (including v2-legacy correct SHA), PKCE E2E scenarios, Auth Health Dashboard rendering, and understands the release gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub raw URL -> LMS plugin installer | v2-legacy repo.xml serves as a static download pointer |
| v3.0-auth merge -> main | Code integration boundary -- all v3.0 changes enter main |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-09 | Tampering | v2-legacy repo.xml static file | low | accept | The repo.xml on v2-legacy is a frozen snapshot from main HEAD (correct SHA af5f2dd0) pointing at a specific GitHub Release asset (v2.3.18). The SHA in repo.xml is the LMS-standard integrity check. No maintenance planned for the branch (D-06). |
| T-54-10 | Tampering | Merge commit integrity | medium | mitigate | Use --no-ff merge to create an explicit merge commit with both parent hashes visible in git log. Post-merge verification (checkpoint Task 3) confirms install.xml and repo.xml landed on 3.0.0. No force-push to main. |
| T-54-11 | Repudiation | Version mismatch between install.xml and repo.xml | medium | mitigate | Checkpoint Task 3 explicitly verifies both files show 3.0.0 before any release action. The spoton-release skill independently validates version consistency at release time. |
| T-54-12 | Information Disclosure | Premature main push exposes version mismatch | medium | mitigate | Main is explicitly NOT pushed until release approval. Acceptance criterion verifies git log origin/main..main shows unpushed commits. Checkpoint Task 3 reminds user of the release gate. |
</threat_model>

<verification>
- git branch -a | grep v2-legacy -- branch exists
- git show v2-legacy:repo.xml | grep af5f2dd0 -- correct SHA (not 0a6849ca)
- git show v2-legacy:repo.xml | grep v2.3.18 -- correct URL (not v2.3.17)
- git show HEAD:Plugins/SpotOn/install.xml | grep '3.0.0' -- version correct
- git show HEAD:repo.xml | grep '3.0.0' -- version correct
- git log --merges -1 shows the v3.0-auth merge commit
- git log origin/main..main | wc -l -- shows unpushed commits (main not pushed)
- PKCE E2E scenarios 1-4 verified by human on dev/test LMS
</verification>

<success_criteria>
v2-legacy branch provides a stable fallback for v2.3.x users (with correct repo.xml SHA/URL). Main branch carries the complete v3.0 auth overhaul with version 3.0.0 but is NOT pushed to origin until release is approved. PKCE migration works end-to-end for all documented scenarios. Ready for release pending user approval.
</success_criteria>

<output>
Create `.planning/phases/54-auth-health-dashboard/54-05-SUMMARY.md` when done
</output>
