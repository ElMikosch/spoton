---
phase: 54-auth-health-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/API/Credentials.pm
  - t/22_audio_key_classifier.t
  - Plugins/SpotOn/Unified/DaemonManager.pm
  - Plugins/SpotOn/API/TokenManager.pm
autonomous: true
requirements: ["D-01", "D-02", "D-03"]
must_haves:
  truths:
    - "classifyAudioKeyError returns 'denied' for permanent audio-key denial stderr signature"
    - "classifyAudioKeyError returns 'throttled' for transient rapid-skip stderr signature"
    - "classifyAudioKeyError returns undef for unrecognized stderr or undef input"
    - "Every alive daemon's stderr is classified on each _streamAlivePoll tick"
    - "Audio-key state is persisted in cache keyed by accountId, not by player MAC"
    - "Denied state uses TTL 'never' (permanent cohort property); throttled state uses TTL 600 (transient, auto-clears)"
    - "Cache write is guarded: only writes when classified state differs from the currently cached value"
    - "Per-account last-API-call timestamp is written on every TokenManager::getToken call (not on _refreshToken)"
  artifacts:
    - "Plugins/SpotOn/API/Credentials.pm with classifyAudioKeyError method"
    - "t/22_audio_key_classifier.t with at least 6 subtests"
    - "Plugins/SpotOn/Unified/DaemonManager.pm with unconditional stderr scan in _streamAlivePoll and $cache instance"
    - "Plugins/SpotOn/API/TokenManager.pm with last-API-call cache write in getToken"
  key_links:
    - "DaemonManager._streamAlivePoll -> Credentials.classifyAudioKeyError -> cache spoton_audiokey_state_$id"
    - "TokenManager.getToken -> cache spoton_last_api_call_$id"
---

<objective>
Create the backend state layer for the Auth Health Dashboard (D-01, D-02, D-03).

Three new pieces of state:
1. Audio-key cohort classification from daemon stderr (D-02 -- passive indicator, no active probe)
2. Per-account last-API-call timestamp (D-01 PKCE indicator sub-field)
3. Unconditional periodic stderr scan hook in DaemonManager (feeds the audio-key classifier)

All reads are passive (cache-backed) so the dashboard (Plan 02) can display them without triggering any active probes or API calls.

Purpose: The dashboard (Plan 02) needs backend state that does not exist yet. Four of the five D-01 indicators already have query methods (needsReauth, accountNeedsMigration, WebPlayer::state, DaemonManager::helperInstances). This plan fills the two gaps: audio-key classification and last-API-call timestamp.

Output: Credentials.pm gains classifyAudioKeyError; DaemonManager._streamAlivePoll gains unconditional stderr scan with properly instantiated $cache; TokenManager.getToken gains last-API-call cache write; new t/22_audio_key_classifier.t unit test.

Addresses review concerns: throttled TTL finite (not 'never'), $cache instantiation in DaemonManager, lastApiCall write only in getToken (not _refreshToken), cache write-guard against identical repetition.
</objective>

## Artifacts this phase produces

- `Plugins::SpotOn::API::Credentials::classifyAudioKeyError($class, $stderrText)` -- new class method returning 'denied'|'throttled'|undef
- Cache key `spoton_audiokey_state_${accountId}` -- persisted audio-key cohort state ('denied' with TTL 'never', 'throttled' with TTL 600)
- Cache key `spoton_last_api_call_${accountId}` -- epoch timestamp of last getToken call
- `t/22_audio_key_classifier.t` -- new test file
- `$cache` instance in DaemonManager.pm (following SPOTON_CACHE_VERSION pattern from TokenManager.pm:34)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-auth-health-dashboard/54-CONTEXT.md
@.planning/phases/54-auth-health-dashboard/54-RESEARCH.md
@Plugins/SpotOn/API/Credentials.pm
@Plugins/SpotOn/Unified/DaemonManager.pm
@Plugins/SpotOn/API/TokenManager.pm
@t/16_credentials.t
@vault/research/Audio Key Service.md
@vault/auth/Audio Key Problem.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audio-key stderr classifier + unit tests</name>
  <files>Plugins/SpotOn/API/Credentials.pm, t/22_audio_key_classifier.t</files>
  <read_first>
    - Plugins/SpotOn/API/Credentials.pm (current state -- isCredentialError at lines 247-256 is the 1:1 model)
    - t/16_credentials.t (test stub pattern -- BEGIN block, Slim::Utils::Cache mock, etc.)
    - vault/research/Audio Key Service.md (the two stderr signatures: 0 1 = permanent denial, 0 2 = transient throttle)
    - vault/auth/Audio Key Problem.md (context: daemon stays alive during audio-key denial, only decryption fails)
  </read_first>
  <behavior>
    - classifyAudioKeyError("error audio key 0 1") returns 'denied'
    - classifyAudioKeyError("lots of output\nerror audio key 0 1\nmore output") returns 'denied'
    - classifyAudioKeyError("error audio key 0 2") returns 'throttled'
    - classifyAudioKeyError("some random stderr text") returns undef
    - classifyAudioKeyError(undef) returns undef
    - classifyAudioKeyError("") returns undef
    - When both signatures present ("error audio key 0 1\nerror audio key 0 2"), returns 'denied' (permanent takes priority)
  </behavior>
  <action>
    RED: Create t/22_audio_key_classifier.t with test cases for all 7 behaviors listed above.
    Follow the test stub pattern from t/16_credentials.t (BEGIN block with Plugins::SpotOn::Plugin
    constant stub, require Plugins::SpotOn::API::Credentials). Run prove -- tests must FAIL (method
    does not exist yet).

    GREEN: Add classifyAudioKeyError($class, $stderrText) to Credentials.pm, placed directly after
    isCredentialError (line 256). The method returns undef unless $stderrText is defined. It checks
    for the permanent denial signature first (regex matching the literal string from librespot-core:
    the exact pattern documented in vault/research/Audio Key Service.md for error code 0 followed
    by sub-code 1), then the transient throttle signature (error code 0, sub-code 2). Permanent
    denial takes priority when both are present.

    The method MUST follow the same discipline as isCredentialError: pure string-matching classifier
    over bounded input, returns only a symbolic enum value, never the raw stderr text. Per D-02,
    this is a passive indicator -- the classifier is called with existing stderrTail output, never
    triggers a new process spawn.
  </action>
  <verify>
    <automated>prove -l t/22_audio_key_classifier.t</automated>
  </verify>
  <acceptance_criteria>
    - Credentials.pm contains sub classifyAudioKeyError
    - classifyAudioKeyError appears after isCredentialError in source order
    - Method signature matches: my ($class, $stderrText) = @_
    - Return values are exactly the strings 'denied', 'throttled', or undef (not 0/1)
    - t/22_audio_key_classifier.t has at least 7 subtests covering all documented behaviors
    - prove -l t/22_audio_key_classifier.t exits 0 with all subtests passing
  </acceptance_criteria>
  <done>classifyAudioKeyError returns the correct symbolic state for each documented stderr signature, unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Unconditional daemon stderr scan + last-API-call timestamp</name>
  <files>Plugins/SpotOn/Unified/DaemonManager.pm, Plugins/SpotOn/API/TokenManager.pm</files>
  <read_first>
    - Plugins/SpotOn/Unified/DaemonManager.pm (full file -- _streamAlivePoll at line 326, crash branch at line 337, alive branch at line 353, health-check-count at line 358, STDERR_TAIL_BYTES constant at line 38, $helper->_accountId accessor at line 112; note: module does NOT declare $cache or use Slim::Utils::Cache -- these must be added)
    - Plugins/SpotOn/API/TokenManager.pm (line 10: use Slim::Utils::Cache; line 34: my $cache = Slim::Utils::Cache->new('spoton', SPOTON_CACHE_VERSION) -- this is the pattern to copy for DaemonManager; getToken at line 52, cache-hit return at line 58, REAUTH_FLAG_PREFIX pattern for cache key naming convention)
    - Plugins/SpotOn/Unified/Daemon.pm (stderrTail method at line 524 -- confirms the method accepts a byte count argument)
  </read_first>
  <action>
    DaemonManager.pm -- FIRST: Add cache instance. DaemonManager.pm currently does NOT import
    Slim::Utils::Cache and has no $cache variable (review finding: this was incorrectly assumed
    to exist). Add these two lines following the pattern from TokenManager.pm:34:
    1. Add "use Slim::Utils::Cache;" to the use-block (after the existing use Slim::Utils::Prefs line)
    2. Add "my $cache = Slim::Utils::Cache->new('spoton', Plugins::SpotOn::Plugin::SPOTON_CACHE_VERSION());"
       in the module-level my-variables section (after the existing $log declaration at line 42)

    DaemonManager.pm -- _streamAlivePoll (line 326): In the alive branch (after the existing debug
    log at line 354, before the health-check-count block at line 357), add an unconditional
    audio-key classification call. For each alive $helper:
    1. require Plugins::SpotOn::API::Credentials (already required at line 342 in the crash branch)
    2. Read my $tail = $helper->stderrTail(STDERR_TAIL_BYTES)
    3. Call my $state = Plugins::SpotOn::API::Credentials->classifyAudioKeyError($tail)
    4. If the classifier returns a defined value AND the helper has an _accountId:
       - Read the currently cached value: my $cached = $cache->get("spoton_audiokey_state_" . $helper->_accountId)
       - Only write if the new state differs from cached: write-guard against identical repetition
         (addresses review concern about cache write per 5s poll)
       - For 'denied': $cache->set("spoton_audiokey_state_" . $helper->_accountId, 'denied', 'never')
         -- permanent cohort property, persists until explicitly cleared
       - For 'throttled': $cache->set("spoton_audiokey_state_" . $helper->_accountId, 'throttled', 600)
         -- transient rapid-skip state (~2min actual, 600s TTL gives generous margin for auto-clear;
         addresses review concern that 'never' TTL for throttled has no recovery path to 'ok')
    5. Do NOT write to cache if classifier returns undef -- preserve any existing 'denied'
       state that may have scrolled out of the stderr tail buffer. The permanent denial
       is an account-level property that should only be set, never cleared by absence.

    TokenManager.pm -- getToken (line 52): Add a per-account last-API-call timestamp at ONE site:
    After the cache-hit early return (line 58, inside the if-block before return $cb->($token)):
    $cache->set("spoton_last_api_call_" . $accountId, time(), 86400)

    Do NOT add a second write site in _refreshToken's success callback. Addresses review concern:
    _refreshToken is called by refreshAllTokens every 45 minutes. Writing lastApiCall there would
    make the timestamp reflect "token refresh cycle alive" rather than "actual API usage." The
    getToken cache-hit path is the single choke point every real API call passes through (all
    40+ Client.pm call sites go through getToken to obtain a token before making an API request),
    making it the correct measurement point for "last API call."

    The 86400 TTL (24 hours) means "last API call" naturally expires to show "no recent activity"
    if the account goes dormant. This is deliberate -- stale timestamps are worse than missing ones.
  </action>
  <verify>
    <automated>prove -l t/07_token_manager.t t/05_perl_syntax.t</automated>
  </verify>
  <acceptance_criteria>
    - DaemonManager.pm contains "use Slim::Utils::Cache" and a $cache declaration using SPOTON_CACHE_VERSION
    - DaemonManager.pm _streamAlivePoll calls classifyAudioKeyError for alive helpers (grep confirms call site)
    - DaemonManager.pm writes 'denied' with TTL 'never' and 'throttled' with TTL 600 (different TTLs, not both 'never')
    - DaemonManager.pm contains a write-guard comparing new state against cached value before writing
    - DaemonManager.pm only writes to cache when classifier returns defined (conditional guard visible)
    - TokenManager.pm getToken writes to cache key matching pattern spoton_last_api_call_ (exactly ONE write site, not two)
    - grep -c 'spoton_last_api_call_' Plugins/SpotOn/API/TokenManager.pm returns exactly 1
    - prove -l t/07_token_manager.t exits 0 (no regression)
    - prove -l t/05_perl_syntax.t exits 0 (all modules compile)
  </acceptance_criteria>
  <done>Every alive daemon's stderr is classified for audio-key state on each poll tick with appropriate TTLs (denied=permanent, throttled=transient); per-account last-API-call timestamp recorded on every getToken invocation only</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| librespot stderr -> Perl classifier | Untrusted process output -- librespot stderr could contain attacker-influenced strings from malicious/compromised upstream Spotify responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-01 | Information Disclosure | classifyAudioKeyError return value | medium | mitigate | Classifier returns only symbolic enum values ('denied'/'throttled'/undef), never raw stderr text -- same discipline as existing isCredentialError (Credentials.pm line 247) |
| T-54-04 | Tampering | spoton_audiokey_state_ cache key | low | accept | Cache is server-local (SQLite-backed Slim::Utils::Cache), not exposed to network. An attacker who can write to the LMS cache has already compromised the server. |
| T-54-05 | Denial of Service | _streamAlivePoll stderrTail read | low | accept | stderrTail is already bounded by STDERR_TAIL_BYTES constant (existing pattern), no new unbounded read introduced. Write-guard prevents redundant cache writes every 5s poll. |
</threat_model>

<verification>
- prove -l t/22_audio_key_classifier.t -- all subtests pass
- prove -l t/07_token_manager.t -- no regression
- prove -l t/05_perl_syntax.t -- all modules compile
- grep -c 'classifyAudioKeyError' Plugins/SpotOn/API/Credentials.pm -- returns >= 1
- grep -c 'spoton_audiokey_state_' Plugins/SpotOn/Unified/DaemonManager.pm -- returns >= 1
- grep -c 'Slim::Utils::Cache' Plugins/SpotOn/Unified/DaemonManager.pm -- returns >= 1 (cache properly instantiated)
- grep -c 'spoton_last_api_call_' Plugins/SpotOn/API/TokenManager.pm -- returns exactly 1 (single write site)
</verification>

<success_criteria>
Audio-key cohort classification (with appropriate TTLs: 'denied'=permanent, 'throttled'=transient auto-clear) and last-API-call timestamp are persisted in cache, readable passively by the dashboard (Plan 02) without triggering any API calls or process spawns.
</success_criteria>

<output>
Create `.planning/phases/54-auth-health-dashboard/54-01-SUMMARY.md` when done
</output>
