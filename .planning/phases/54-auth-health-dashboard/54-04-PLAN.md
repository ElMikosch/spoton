---
phase: 54-auth-health-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["54-03"]
files_modified:
  - Plugins/SpotOn/Plugin.pm
autonomous: true
requirements: ["D-04", "D-05"]
must_haves:
  truths:
    - "Playlist play-all works correctly via the chosen mechanism (unless Option C was chosen, in which case dead code is cleanly removed)"
    - "Temporary Plan-03 debug logging (DIAG.*index=.*quantity= signature) is removed"
    - "Existing Pause-Bug DIAG lines in Plugin.pm are UNTOUCHED (6 existing [DIAG] lines in Watchdog/PauseGuard/ModeChange blocks)"
    - "The _fetchAllPages pagination engine is untouched (only the trigger changes)"
    - "Saved Tracks and Albums play-all behavior is consistent with the playlist fix"
  artifacts:
    - "Plugins/SpotOn/Plugin.pm with the chosen fix applied and Plan-03 debug logging removed"
  key_links:
    - "Plugin.pm _playlistFeed play-all trigger -> _fetchAllPages pagination engine"
---

<objective>
Implement the fix approach chosen in Plan 03's checkpoint decision and remove temporary debug logging (D-04, D-05).

This plan adapts to the user's chosen fix approach:
- Option A: Replace the quantity-based heuristic with a different detection mechanism
- Option B: Add an explicit "Play All" child menu item with a passthrough flag
- Option C: Remove the unreachable $qty >= 500 code paths (dead code cleanup) -- NOTE: if Option C is chosen, must_haves truth "Playlist play-all works correctly" becomes "Dead code removed, behavior accepted as-is (matches Spotty-NG)" and the CHANGELOG Fixed entry is replaced with a Changed entry documenting the behavior clarification.

Regardless of option, the _fetchAllPages pagination engine itself (line 1584) is NOT modified -- only the trigger mechanism changes. Per D-05, apply the fix consistently across all 4 feed functions that have the >= 500 heuristic.

CRITICAL: When removing Plan-03 DIAG lines, only remove the 4 lines matching the specific Plan-03 signature (DIAG.*index=.*quantity=). Plugin.pm contains 6 OTHER [DIAG] lines (Watchdog, PauseGuard, ModeChange blocks at lines 2921-3036) that are deliberately preserved Pause-Bug diagnostics per project decision -- these MUST NOT be touched.

Purpose: Fix the playlist play-all bug based on confirmed root cause and user-chosen approach.
Output: Plugin.pm with the fix applied and Plan-03 debug logging cleaned up.

Addresses review concern: DIAG-grep acceptance criterion narrowed to Plan-03-specific signature to avoid collateral damage to existing Pause-Bug diagnostics.
</objective>

## Artifacts this phase produces

- Modified play-all trigger logic in _playlistFeed, _savedTracksFeed, _albumFeed, _showFeed
- (If option B) New OPML child item in playlist/album/tracks list feeds

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-auth-health-dashboard/54-CONTEXT.md
@.planning/phases/54-auth-health-dashboard/54-RESEARCH.md
@.planning/phases/54-auth-health-dashboard/54-03-SUMMARY.md
@Plugins/SpotOn/Plugin.pm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement chosen play-all fix</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (the 4 feed functions with >= 500 heuristic):
      - _savedTracksFeed: grep for "$qty >= 500" near line 1433
      - _showFeed: grep for "$qty >= 500" near line 1759
      - _albumFeed: grep for "$qty >= 500" near line 2458
      - _playlistFeed: grep for "$qty >= 500" near line 2747
    - Plugins/SpotOn/Plugin.pm (_fetchAllPages at line 1584 -- understand its interface: accountId, apiFn, pageLimit, extractItems, done callback)
    - .planning/phases/54-auth-health-dashboard/54-03-SUMMARY.md (read the checkpoint decision result to know which option was chosen)
  </read_first>
  <action>
    Read the Plan 03 SUMMARY to determine which fix option was chosen.

    IF OPTION A (replace quantity heuristic):
    Replace the $qty >= 500 condition in all 4 feed functions with the alternative detection
    mechanism specified in the checkpoint decision notes. Ensure _fetchAllPages is called with
    the same arguments as before -- only the trigger condition changes.

    IF OPTION B (explicit Play All child item):
    In each of the 4 list-level feed functions (the functions that RETURN the list of
    items -- _savedTracksFeed entry, _showFeed entry, _albumFeed entry, and the Playlists
    list builder), add an explicit OPML child item (type => 'audio', name => 'Play All',
    or equivalent) that routes through a passthrough flag (e.g., playAll => 1) to the
    existing _fetchAllPages path. The existing $qty >= 500 heuristic can then be replaced
    with a check for $passthrough->{playAll}. This makes play-all a first-class user action
    rather than an implicit quantity-threshold inference.

    IF OPTION C (accept current behavior, dead code cleanup):
    Remove the $qty >= 500 conditional blocks from all 4 feed functions since they are
    unreachable from any play command path (confirmed by research + debug logging). Keep
    the _playAllItemCache and _fetchAllPages code intact for potential future use, but
    remove the dead trigger paths. Document the removal with a brief comment explaining
    the LMS core quantity-stripping mechanism that makes this code unreachable.
    NOTE (addresses review concern): If Option C is chosen, update the CHANGELOG entry
    in Plan 05 to use "Changed" not "Fixed" for the playlist behavior, since no fix was
    applied -- only dead code was removed and behavior was accepted.

    In ALL cases: apply the fix consistently across _savedTracksFeed, _showFeed, _albumFeed,
    and _playlistFeed. D-05 notes the bug only manifests for playlists, but all 4 functions
    share the same heuristic structure and should be treated uniformly.
  </action>
  <verify>
    <automated>prove -l t/05_perl_syntax.t</automated>
  </verify>
  <acceptance_criteria>
    - If option A/B: The play-all trigger in all 4 feed functions no longer uses $qty >= 500 as the sole condition
    - If option C: No $qty >= 500 conditional exists in Plugin.pm
    - _fetchAllPages function (line 1584) is unchanged
    - prove -l t/05_perl_syntax.t exits 0
  </acceptance_criteria>
  <done>Play-all trigger is fixed per the chosen approach, applied consistently across all 4 feed functions</done>
</task>

<task type="auto">
  <name>Task 2: Remove temporary Plan-03 debug logging</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (grep for the Plan-03-specific DIAG signature to find the 4 temporary log lines; ALSO grep for existing [DIAG] lines in the Watchdog/PauseGuard/ModeChange blocks at lines 2921-3036 to confirm they exist and must NOT be removed)
  </read_first>
  <action>
    Remove all 4 temporary DIAG log lines added in Plan 03 Task 1. These lines match the
    SPECIFIC signature: DIAG.*index=.*quantity= (the Plan-03-unique pattern).

    CRITICAL: Plugin.pm contains 6 OTHER [DIAG] lines in the Watchdog/PauseGuard/ModeChange
    blocks (around lines 2921-3036). These are deliberately preserved Pause-Bug diagnostics
    per project decision (STATE.md: "v2.1.6 DIAG-Logging entfernen wenn Pause-Bug geloest").
    Do NOT remove these lines. They are identifiable by their different signature pattern
    ([DIAG] Watchdog:, [DIAG] PauseGuard:, [DIAG] ModeChange:) and are guarded by
    $prefs->get('diagnosticMode').

    Only remove lines matching the Plan-03 pattern (DIAG followed by index= and quantity=).
  </action>
  <verify>
    <automated>grep -v '^#' Plugins/SpotOn/Plugin.pm | grep -c 'DIAG.*index=.*quantity='</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' Plugins/SpotOn/Plugin.pm | grep -c 'DIAG.*index=.*quantity=' returns 0 (Plan-03 lines removed)
    - grep -c '\[DIAG\]' Plugins/SpotOn/Plugin.pm returns 6 (existing Pause-Bug diagnostics preserved)
    - prove -l t/05_perl_syntax.t exits 0
  </acceptance_criteria>
  <done>Plan-03 temporary debug logging removed; existing Pause-Bug DIAG lines preserved intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Fix modifies internal feed function logic, no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-08 | Denial of Service | _fetchAllPages full pagination | low | accept | _fetchAllPages already exists and has been in production since Phase 25. The fix changes only the TRIGGER, not the pagination engine itself. Rate limiting is handled by the existing Client.pm throttle. No new DoS surface. |
</threat_model>

<verification>
- prove -l t/05_perl_syntax.t -- passes
- grep -v '^#' Plugins/SpotOn/Plugin.pm | grep -c 'DIAG.*index=.*quantity=' -- returns 0 (Plan-03 lines gone)
- grep -c '\[DIAG\]' Plugins/SpotOn/Plugin.pm -- returns 6 (Pause-Bug diagnostics preserved)
- prove -l t/ -- full suite passes (no regression)
</verification>

<success_criteria>
Playlist play-all works via the chosen mechanism (or behavior accepted if Option C). Plan-03 debug logging removed without touching existing Pause-Bug diagnostics. No regression in browse/search/library functionality.
</success_criteria>

<output>
Create `.planning/phases/54-auth-health-dashboard/54-04-SUMMARY.md` when done
</output>
