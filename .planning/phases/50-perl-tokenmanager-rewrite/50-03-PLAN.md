---
phase: 50-perl-tokenmanager-rewrite
plan: 03
type: execute
wave: 2
depends_on: [50-01, 50-02]
files_modified:
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
  - t/02_strings.t
autonomous: true
requirements: [AUTH-05]

must_haves:
  truths:
    - "Plugin.pm handleFeed() shows OPML warning item when any account needs re-auth (D-08 Channel 1)"
    - "Settings.pm handler() sets $paramRef->{'warning'} when any account needs re-auth (D-08 Channel 2)"
    - "Settings.pm _pkceStoreAccount() calls TokenManager->clearNeedsReauth($accountId) on success (clears stale flag immediately)"
    - "Plugin.pm has no _autoStartDiscovery timer or sub (D-01)"
    - "Plugin.pm _killOrphanedProcesses no longer references discoveryPid"
    - "Settings.pm has no startDiscovery/stopDiscovery call sites or handler subs"
    - "Settings.pm sub _autoSetupAccount and the __DISCOVER__ fallback block in handler() are deleted (M-3)"
    - "basic.html has no discovery_widget BLOCK or spotonDiscovery() JS function"
    - "Add Another Account button in basic.html uses spotonPkceStart() XHR call, not window.location (M-2)"
    - "Plugin.pm rate-limit hint uses single cache key spoton_rate_limit (no _own/_bundled)"
    - "Settings.pm diagnostics dump uses rateLimited key, not rateLimitedOwn/rateLimitedBundled (M-7)"
    - "strings.txt contains PLUGIN_SPOTON_REAUTH_REQUIRED with EN and DE translations"
    - "t/02_strings.t @bilingual_keys includes PLUGIN_SPOTON_REAUTH_REQUIRED"
  artifacts:
    - Plugins/SpotOn/Plugin.pm
    - Plugins/SpotOn/Settings.pm
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
    - Plugins/SpotOn/strings.txt
    - t/02_strings.t
  key_links:
    - "Plugin.pm handleFeed() -> TokenManager.anyAccountNeedsReauth() -> OPML warning item"
    - "Settings.pm handler() -> TokenManager.anyAccountNeedsReauth() -> $paramRef->{'warning'}"
    - "Settings.pm _pkceStoreAccount() -> TokenManager.clearNeedsReauth() (clear stale flag on re-auth)"
    - "basic.html Add Another Account button -> spotonPkceStart() -> /plugins/SpotOn/settings/pkce/start POST (PKCE flow)"
---

<objective>
Wire the re-auth notification Channels 1 (OPML menu) and 2 (Settings page) into Plugin.pm
and Settings.pm. Remove all ZeroConf discovery code from Plugin.pm, Settings.pm, and
basic.html (D-01), including _autoSetupAccount and the __DISCOVER__ fallback block (M-3).
Repoint the "Add Another Account" button from discovery to the PKCE flow using the existing
spotonPkceStart() XHR pattern (M-2), not window.location navigation. Fix Settings.pm
diagnostics dump rate-limit keys (M-7). Add i18n strings for re-auth warnings.

Incorporates Fable 5 review findings: M-2 (spotonPkceStart XHR not window.location),
M-3 (_autoSetupAccount + __DISCOVER__ block deletion), M-7 (Settings.pm diagnostics
rate-limit key fix), T-50-11 correction (cache is SQLite-persistent, not memory-only).

Purpose: Complete the 4-channel re-auth notification system (D-08) and eliminate all
discovery-as-auth-mechanism references (D-01). Preserve multi-account support by repointing
to PKCE flow.

Output: Wired Plugin.pm + Settings.pm + basic.html + i18n strings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-perl-tokenmanager-rewrite/50-RESEARCH.md
@.planning/phases/50-perl-tokenmanager-rewrite/50-PATTERNS.md
@.planning/phases/50-perl-tokenmanager-rewrite/50-REVIEWS.md
@.planning/phases/50-perl-tokenmanager-rewrite/50-01-SUMMARY.md
@.planning/phases/50-perl-tokenmanager-rewrite/50-02-SUMMARY.md
@Plugins/SpotOn/Plugin.pm
@Plugins/SpotOn/Settings.pm
@Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

### Modified: Plugins/SpotOn/Plugin.pm
- `handleFeed()` -- added re-auth OPML warning item before account-switcher block
  (calls TokenManager->anyAccountNeedsReauth(), pushes {name => cstring($client, 'PLUGIN_SPOTON_REAUTH_REQUIRED'), type => 'textarea'})
- `handleFeed()` -- rate-limit hint uses single cache key 'spoton_rate_limit' (no _own/_bundled)
- `_killOrphanedProcesses()` -- removed discoveryPid guard references

### Deleted from Plugin.pm
- `sub _autoStartDiscovery` and its 2-line timer setup in initPlugin

### Modified: Plugins/SpotOn/Settings.pm
- `handler()` -- added $paramRef->{'warning'} = string('PLUGIN_SPOTON_REAUTH_REQUIRED_SETTINGS')
  when anyAccountNeedsReauth() is true
- `handler()` -- diagnostics dump rate-limit lines use 'rateLimited' key from
  statusSnapshot (M-7)
- `_pkceStoreAccount()` -- added TokenManager->clearNeedsReauth($accountId) call on
  successful account (re-)storage

### Deleted from Settings.pm
- Discovery AJAX handler subs (_discoveryStartHandler, _discoveryStopHandler, _discoveryStatusHandler)
- Discovery handler registrations
- _isDiscoveryRunning() helper sub
- $paramRef->{discoveryRunning} assignment
- startDiscovery()/stopDiscovery() call sites
- elsif (_isDiscoveryRunning()) branch in handler()
- sub _autoSetupAccount (lines 261-314) -- M-3
- The __DISCOVER__ credentials.json fallback block in handler() (lines 211-219) -- M-3

### Modified: basic.html
- Removed BLOCK discovery_widget entirely (both definition and all INCLUDE calls)
- Removed spotonDiscovery() JS function
- Removed ZEROCONF_SETUP wrapper section for no-accounts state
- "Add Another Account" button repointed to use onclick="spotonPkceStart()" -- the same
  XHR POST pattern as the existing "Connect Spotify Account" button (M-2). NOT
  window.location navigation (which would GET a JSON-responding endpoint).

### Added to strings.txt
- PLUGIN_SPOTON_REAUTH_REQUIRED -- 11-language translations for OPML menu warning
- PLUGIN_SPOTON_REAUTH_REQUIRED_SETTINGS -- 11-language translations for Settings page warning

### Modified: t/02_strings.t
- @bilingual_keys array extended with PLUGIN_SPOTON_REAUTH_REQUIRED and
  PLUGIN_SPOTON_REAUTH_REQUIRED_SETTINGS
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Plugin.pm -- discovery removal + OPML reauth warning + rate-limit key simplification</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    Plugins/SpotOn/Plugin.pm
    Plugins/SpotOn/API/TokenManager.pm (Plan 01 output -- anyAccountNeedsReauth/needsReauth API)
    .planning/phases/50-perl-tokenmanager-rewrite/50-RESEARCH.md (Pattern 3: OPML Menu Warning)
    .planning/phases/50-perl-tokenmanager-rewrite/50-PATTERNS.md (Plugin.pm section)
    .planning/phases/50-perl-tokenmanager-rewrite/50-REVIEWS.md (no Plugin.pm-specific findings beyond L-7)
    .planning/phases/50-perl-tokenmanager-rewrite/50-01-SUMMARY.md (TokenManager API changes)
  </read_first>
  <action>
    **DELETE _autoStartDiscovery and its timer (D-01):**
    Remove the 2-line timer setup in initPlugin (lines 182-183):
    Slim::Utils::Timers::killTimers(undef, \&_autoStartDiscovery) and the setTimer call.
    Delete sub _autoStartDiscovery entirely (lines 298-303).

    **UPDATE _killOrphanedProcesses (lines ~355-398):**
    Remove the discoveryPid variable and its assignment via TokenManager->discoveryPid
    (lines 355-357). Remove the two guards that check "$discoveryPid && $pid == $discoveryPid"
    (lines 374 and 398). The orphan-killing logic itself stays (kills leftover spoton
    processes) -- just the discovery-process-skip guard goes away. Since discovery/ZeroConf
    as a feature stays for guest-discovery (v3.0 decisions) but is no longer managed by
    TokenManager (it is librespot's own mDNS, not a TokenManager subprocess), this guard
    is dead code.

    **ADD OPML re-auth warning in handleFeed (D-08 Channel 1):**
    Before the rate-limit hint block (line ~432), add a re-auth check following the
    exact same conditional-push pattern. Use require to avoid load-order issues:
    require Plugins::SpotOn::API::TokenManager;
    if (Plugins::SpotOn::API::TokenManager->anyAccountNeedsReauth()) {
        push @items, {
            name => cstring($client, 'PLUGIN_SPOTON_REAUTH_REQUIRED'),
            type => 'textarea',
        };
    }
    This renders as a non-actionable text item in the OPML menu tree, visible on all
    players, directing the user to Settings for re-authentication.

    **SIMPLIFY rate-limit hint cache key (lines ~432-436):**
    Change from: if ($cache->get('spoton_rate_limit_own') || $cache->get('spoton_rate_limit_bundled'))
    To: if ($cache->get('spoton_rate_limit'))
    This matches the single key in the simplified Client.pm from Plan 02.
  </action>
  <verify>
    <automated>perl -c -I lib -I Plugins Plugins/SpotOn/Plugin.pm 2>&amp;1 | grep -q 'syntax OK' &amp;&amp; echo "SYNTAX OK" || echo "SYNTAX FAIL"; grep -v '^#' Plugins/SpotOn/Plugin.pm | grep -c '_autoStartDiscovery\|discoveryPid\|spoton_rate_limit_own\|spoton_rate_limit_bundled'</automated>
  </verify>
  <acceptance_criteria>
    - Plugin.pm compiles cleanly (perl -c exits 0)
    - No _autoStartDiscovery sub or timer references exist (grep returns 0)
    - No discoveryPid references exist in non-comment lines
    - handleFeed contains anyAccountNeedsReauth check with PLUGIN_SPOTON_REAUTH_REQUIRED string key
    - Rate-limit hint uses single key 'spoton_rate_limit' (no _own/_bundled)
    - _killOrphanedProcesses still kills orphan spoton processes (functionality preserved)
    - _refreshAllTokens timer and sub unchanged (still calls TokenManager->refreshAllTokens)
  </acceptance_criteria>
  <done>Plugin.pm has no discovery-as-auth code, shows OPML re-auth warning when accounts need re-authentication, and uses simplified rate-limit cache key.</done>
</task>

<task type="auto">
  <name>Task 2: Settings.pm + basic.html + i18n -- discovery removal, _autoSetupAccount deletion, Settings warning, Add Another Account repoint, diagnostics fix</name>
  <files>Plugins/SpotOn/Settings.pm, Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html, Plugins/SpotOn/strings.txt, t/02_strings.t</files>
  <read_first>
    Plugins/SpotOn/Settings.pm
    Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
    Plugins/SpotOn/strings.txt (lines 105-140 for existing RATE_LIMIT_HINT/ACCOUNT_NONE pattern)
    t/02_strings.t (lines 25-130 for @bilingual_keys array)
    .planning/phases/50-perl-tokenmanager-rewrite/50-RESEARCH.md (Pattern 4, Pitfall 5, Open Question 2)
    .planning/phases/50-perl-tokenmanager-rewrite/50-PATTERNS.md (Settings.pm section)
    .planning/phases/50-perl-tokenmanager-rewrite/50-REVIEWS.md (M-2, M-3, M-7)
    .planning/phases/50-perl-tokenmanager-rewrite/50-01-SUMMARY.md (TokenManager clearNeedsReauth API)
  </read_first>
  <action>
    **Settings.pm -- ADD re-auth warning (D-08 Channel 2):**
    In handler(), near the existing $paramRef->{helperMissing} assignment (line ~99), add:
    require Plugins::SpotOn::API::TokenManager;
    if (Plugins::SpotOn::API::TokenManager->anyAccountNeedsReauth()) {
        $paramRef->{'warning'} = string('PLUGIN_SPOTON_REAUTH_REQUIRED_SETTINGS');
    }
    This uses the LMS core $paramRef->{'warning'} idiom (verified from
    Slim::Web::Settings::Server::Software.pm). The settings/header.html template
    (already PROCESSed by basic.html) renders this automatically.

    **Settings.pm -- ADD clearNeedsReauth on successful PKCE re-auth:**
    In _pkceStoreAccount (the success path after a fresh PKCE connect), add:
    Plugins::SpotOn::API::TokenManager->clearNeedsReauth($accountId);
    after the successful account storage. This immediately clears the reauth flag
    when a user re-authenticates, rather than waiting up to 45 minutes for the next
    timer cycle. Note: clearNeedsReauth is a public method (no underscore prefix) per
    Plan 01 (L-6 resolution).

    **Settings.pm -- DELETE discovery handlers and references (D-01, Pitfall 5):**
    - Remove startDiscovery()/stopDiscovery() call sites (lines 218, 350, 365)
    - Remove $paramRef->{discoveryRunning} = _isDiscoveryRunning() ? 1 : 0 (line 225)
    - Remove the elsif (_isDiscoveryRunning()) branch (line 331) -- inspect the
      surrounding control flow: if this branch affects which params are set, ensure the
      remaining branches still cover all states correctly
    - Delete sub _isDiscoveryRunning entirely (lines 973-977)
    - Remove discovery AJAX handler registrations -- grep for the 3 discovery handler
      subs and their Slim::Web::Pages->addPageFunction registrations
    - Delete the 3 discovery AJAX handler subs themselves

    **Settings.pm -- DELETE _autoSetupAccount and __DISCOVER__ block (M-3):**
    DELETE sub _autoSetupAccount entirely (lines 261-314). This sub handles synchronous
    account creation from __DISCOVER__/credentials.json, which is the ZeroConf discovery
    flow being removed per D-01. After removal, no code calls _fetchDisplayName for
    discovery accounts, so the needsReauth false-positive path described in M-3 is
    eliminated. Also DELETE the __DISCOVER__ credentials.json check and _autoSetupAccount
    call in handler() (lines 211-219). The block checks for the existence of
    __DISCOVER__/credentials.json and calls _autoSetupAccount -- both are ZeroConf
    discovery artifacts that have no purpose once discovery-as-auth is removed.

    **Settings.pm -- FIX diagnostics dump rate-limit keys (M-7, lines 829-830):**
    In the diagnostics section of handler() where it builds the @tokenStatus array,
    change lines 829-830 from:
      push @tokenStatus, "  Rate limited (own): " . ($snapshot->{rateLimitedOwn} ? 'YES' : 'no');
      push @tokenStatus, "  Rate limited (bundled): " . ($snapshot->{rateLimitedBundled} ? 'YES' : 'no');
    to a single line:
      push @tokenStatus, "  Rate limited: " . ($snapshot->{rateLimited} ? 'YES' : 'no');
    This matches Plan 02's statusSnapshot change from rateLimitedOwn/rateLimitedBundled
    to a single rateLimited key.

    **basic.html -- DELETE discovery widget and JS (Pitfall 5):**
    - Delete the entire BLOCK discovery_widget definition (lines 4-38 approximately)
    - Delete all [% INCLUDE discovery_widget %] calls (lines 174, 204)
    - Delete the spotonDiscovery() JS function (line 325+)
    - Remove the [% WRAPPER setting title="PLUGIN_SPOTON_ZEROCONF_SETUP" %] block
      (line 173-175) that wraps the discovery widget for no-accounts state

    **basic.html -- REPOINT "Add Another Account" button (M-2):**
    CRITICAL: Do NOT use window.location = '...pkce/start'. The _pkceStartHandler
    (Settings.pm:378-381) expects a POST request with _csrfCheck and responds with JSON
    {url, nonce}, not a redirect. A GET navigation would display raw JSON in the browser.

    Instead, repoint the "Add Another Account" button onclick to call spotonPkceStart()
    -- the same XHR POST function already used by the primary "Connect Spotify Account"
    button (basic.html line 258+). The spotonPkceStart function does an XHR POST to
    /plugins/SpotOn/settings/pkce/start, receives the JSON response, and navigates
    to the returned url. This is the correct CSRF-safe pattern.

    Concretely: replace the onclick="spotonDiscovery('start')" attribute on the
    "Add Another Account" button (line 208) with onclick="spotonPkceStart()".

    **strings.txt -- ADD re-auth i18n keys:**
    Add two new string keys following the existing pattern (11 languages: CS, DA, DE,
    EN, ES, FR, IT, NL, NO, PL, SV):

    PLUGIN_SPOTON_REAUTH_REQUIRED -- OPML menu warning text. Keep short for player
    displays. EN: "Spotify authentication expired -- check Settings"
    DE: "Spotify-Authentifizierung abgelaufen -- siehe Einstellungen"

    PLUGIN_SPOTON_REAUTH_REQUIRED_SETTINGS -- Settings page warning text. Can be longer.
    EN: "One or more Spotify accounts require re-authentication. Click 'Connect Spotify Account' to renew."
    DE: "Ein oder mehrere Spotify-Konten erfordern eine erneute Authentifizierung. Klicken Sie auf 'Spotify-Konto verbinden' zum Erneuern."

    Provide reasonable translations for all 11 languages (matching the style and quality
    of existing translations in the file).

    **t/02_strings.t -- UPDATE @bilingual_keys:**
    Add PLUGIN_SPOTON_REAUTH_REQUIRED and PLUGIN_SPOTON_REAUTH_REQUIRED_SETTINGS to the
    @bilingual_keys array (after the last PKCE key, around line 124).

    **Verify:** prove t/02_strings.t passes. All modified .pm files compile cleanly.
  </action>
  <verify>
    <automated>prove t/02_strings.t t/05_perl_syntax.t 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Settings.pm compiles cleanly (perl -c exits 0)
    - Settings.pm handler() contains $paramRef->{'warning'} assignment with PLUGIN_SPOTON_REAUTH_REQUIRED_SETTINGS
    - Settings.pm _pkceStoreAccount calls clearNeedsReauth on TokenManager
    - Settings.pm has no startDiscovery, stopDiscovery, _isDiscoveryRunning references in non-comment lines
    - Settings.pm has no sub _autoSetupAccount (M-3)
    - Settings.pm handler() has no __DISCOVER__ credentials.json check block (M-3)
    - Settings.pm diagnostics dump uses $snapshot->{rateLimited} not rateLimitedOwn/rateLimitedBundled (M-7)
    - basic.html has no BLOCK discovery_widget, no spotonDiscovery function, no ZEROCONF_SETUP section
    - basic.html "Add Another Account" button uses onclick="spotonPkceStart()" not spotonDiscovery or window.location (M-2)
    - strings.txt contains PLUGIN_SPOTON_REAUTH_REQUIRED with at least EN and DE translations
    - strings.txt contains PLUGIN_SPOTON_REAUTH_REQUIRED_SETTINGS with at least EN and DE translations
    - t/02_strings.t @bilingual_keys includes both new keys
    - prove t/02_strings.t exits 0 (all string tests pass)
    - prove t/05_perl_syntax.t exits 0 (all modified Perl files compile)
  </acceptance_criteria>
  <done>Settings.pm shows re-auth warning banner, clears reauth flag on fresh PKCE auth. All discovery code including _autoSetupAccount removed from Settings.pm and basic.html. "Add Another Account" uses spotonPkceStart() XHR (not window.location). Diagnostics dump uses consolidated rate-limit key. i18n strings added and tested.</done>
</task>

</tasks>

<review_findings_addressed>
## Review Findings Incorporated in This Plan

| Finding | Severity | How Addressed |
|---------|----------|---------------|
| M-2: "Add Another Account" repoint uses window.location but handler expects POST+JSON | MEDIUM | Button repointed to spotonPkceStart() XHR call -- same CSRF-safe POST pattern as primary Connect button |
| M-3: _autoSetupAccount + __DISCOVER__ block not in delete list | MEDIUM | Both deleted from Settings.pm; eliminates false-positive needsReauth path for discovery accounts |
| M-7: Settings.pm:829-830 reads old rateLimitedOwn/rateLimitedBundled | MEDIUM | Diagnostics dump updated to use single rateLimited key from statusSnapshot |
| T-50-11 correction: "Cache-based flag naturally clears on LMS restart" is false | MEDIUM | Statement removed from plan. The spoton cache is SQLite-persistent (DbCache), so the reauth flag persists across restarts. It is explicitly cleared only by clearNeedsReauth on successful refresh or re-auth. |
</review_findings_addressed>

<review_findings_deferred>
## Review Findings Deferred

| Finding | Severity | Reason |
|---------|----------|--------|
| L-3: T-50-06 misattributes accountId validation location | LOW | Factual correction in threat register rationale; does not change the threat disposition or plan actions. No code change needed. Noted for accuracy. |
| L-7: No runtime verification that DSTM/Personal Mixes work with PKCE token after flavor removal | LOW | Spike 002 validated all 21/21 endpoints including browse/categories. DSTM uses recommendations endpoint which is in the Removed list (Nov 2024 API changes), not a flavor issue. getPersonalMixes uses standard search/playlists. A runtime test would require a running LMS instance and Spotify account, which is Phase 53 UAT territory, not a unit-testable concern in Phase 50. |
</review_findings_deferred>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings.pm handler -> LMS web UI | Settings page renders warning via $paramRef->{'warning'} (server-side template, no XSS vector) |
| Plugin.pm handleFeed -> LMS OPML client | OPML items rendered server-side, string via cstring() (i18n-safe) |
| basic.html -> browser | "Add Another Account" uses spotonPkceStart() XHR POST (same-origin, CSRF-checked) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-09 | Spoofing | "Add Another Account" button action | low | accept | Repoints to spotonPkceStart() which does XHR POST to /plugins/SpotOn/settings/pkce/start with _csrfCheck. Same endpoint and pattern as the primary PKCE button. No new attack surface. |
| T-50-10 | Information Disclosure | Re-auth warning leaks account state | low | accept | Warning text only says "authentication expired" -- no account ID, email, or token information exposed in the UI. String is static i18n text. |
| T-50-11 | Denial of Service | Stale re-auth flag shows false warning | medium | mitigate | Settings.pm _pkceStoreAccount calls clearNeedsReauth immediately on successful re-auth. The needsReauth cache flag is SQLite-persistent (spoton cache uses DbCache) with TTL 'never', so it survives restarts but is always explicitly cleared by successful refresh (clearNeedsReauth in _refreshToken) or successful PKCE re-authentication (clearNeedsReauth in _pkceStoreAccount). |
| T-50-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
1. `prove t/02_strings.t t/05_perl_syntax.t` -- string tests and syntax checks pass
2. `prove t/` -- full test suite green (integration verification across all plans)
3. grep for discovery/ZeroConf call sites in Plugin.pm, Settings.pm, basic.html returns 0
4. grep for _autoSetupAccount in Settings.pm returns 0 (M-3)
5. grep for rateLimitedOwn/rateLimitedBundled in Settings.pm returns 0 (M-7)
6. grep for spotonDiscovery in basic.html returns 0
7. grep for spotonPkceStart in basic.html finds "Add Another Account" button
</verification>

<success_criteria>
All 4 re-auth notification channels wired (Channels 1+2 in this plan, Channels 3+4 in Plan 01).
All discovery-as-auth-mechanism code removed from Plugin.pm, Settings.pm, and basic.html,
including _autoSetupAccount and __DISCOVER__ block (M-3). "Add Another Account" uses PKCE
flow via spotonPkceStart() XHR (M-2, multi-account preserved). Settings diagnostics dump
uses consolidated rate-limit key (M-7). i18n strings added for 11 languages and tested.
Full test suite passes.
</success_criteria>

<output>
Create `.planning/phases/50-perl-tokenmanager-rewrite/50-03-SUMMARY.md` when done
</output>
