---
phase: 53-keymaster-removal-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/API/Client.pm
  - Plugins/SpotOn/strings.txt
  - t/02_strings.t
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AUTH-06, AUTH-07]
must_haves:
  truths:
    - "_doFlavouredRequest is renamed to _doRequest throughout Client.pm with no behavioral change"
    - "All 22 existing Keymaster-mentioning i18n strings are rewritten for the PKCE/OAuth context"
    - "New i18n strings for migration hints and auth-required messages exist in all 11 languages"
    - "AUTH-06 is marked as dropped with rationale in REQUIREMENTS.md"
    - "A permanent regression test in t/02_strings.t asserts zero case-insensitive keymaster references in strings.txt"
  artifacts:
    - "Plugins/SpotOn/API/Client.pm with _doRequest (renamed, stale comments fixed)"
    - "Plugins/SpotOn/strings.txt with PKCE-context descriptions and new MIGRATION/AUTH_REQUIRED keys"
    - "t/02_strings.t with keymaster regression assertion"
    - ".planning/REQUIREMENTS.md with AUTH-06 dropped annotation"
  key_links:
    - "_request() calls _doRequest() — single call site, must match"
    - "New string keys must exactly match what Plan 03 references in Plugin.pm/Settings.pm/basic.html"
    - "PLUGIN_SPOTON_CLIENT_ID_WIZARD_TITLE is a NEW key (not PLUGIN_SPOTON_SETUP_GUIDE_TITLE which already exists for the initial setup guide)"
---

<objective>
Perl naming cleanup in Client.pm (D-14), i18n string rewrite (D-15), new i18n strings for migration UX, AUTH-06 disposition update (D-01/D-02), and permanent keymaster regression test.

Purpose: Remove all Keymaster-era naming artifacts from the Perl HTTP client, rewrite user-facing strings from Keymaster context to PKCE/OAuth context, and add new strings that Plan 03 will wire into the migration UX. Mark AUTH-06 as dropped. Add a permanent test assertion preventing future reintroduction of keymaster references.

Output: Cleaned Client.pm, fully rewritten strings.txt, keymaster regression assertion in t/02_strings.t, updated REQUIREMENTS.md.

Review concerns addressed:
- HIGH: PLUGIN_SPOTON_SETUP_GUIDE_TITLE key collision — using new key PLUGIN_SPOTON_CLIENT_ID_WIZARD_TITLE instead (review finding #1)
- MEDIUM: PLUGIN_SPOTON_SETUP_DEVAPP_DESC dual-use — rewrite uses position-neutral wording (review finding #3)
- MEDIUM: No permanent regression tests — adding keymaster assertion to t/02_strings.t (review finding #5, partial)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-keymaster-removal-migration/53-CONTEXT.md
@.planning/phases/53-keymaster-removal-migration/53-RESEARCH.md
@.planning/phases/53-keymaster-removal-migration/53-PATTERNS.md
</context>

## Artifacts this phase produces

Plan 02 artifacts:
- `Client.pm::_doRequest()` method (renamed from `_doFlavouredRequest`)
- i18n key `PLUGIN_SPOTON_MIGRATION_REQUIRED` (11 languages) — used by Plan 03 in Plugin.pm handleFeed
- i18n key `PLUGIN_SPOTON_AUTH_REQUIRED` (11 languages) — used by Plan 03 in Plugin.pm _authRequiredItem
- i18n key `PLUGIN_SPOTON_MIGRATION_SETTINGS_HINT` (11 languages) — used by Plan 03 in basic.html migration banner
- i18n key `PLUGIN_SPOTON_CLIENT_ID_WIZARD_TITLE` (11 languages) — used by Plan 03 in basic.html collapsible summary (NOT the existing PLUGIN_SPOTON_SETUP_GUIDE_TITLE which is the initial setup guide title)
- Rewritten `PLUGIN_SPOTON_CLIENT_ID_DESC` (11 languages) — no longer mentions Keymaster
- Rewritten `PLUGIN_SPOTON_SETUP_DEVAPP_DESC` (11 languages) — position-neutral PKCE setup wizard content (this key is also used in the no-accounts setup guide at basic.html:18, so wording must work in both contexts)
- Regression assertion in `t/02_strings.t` preventing future keymaster reintroduction

## Review finding: key collision prevention

`PLUGIN_SPOTON_SETUP_GUIDE_TITLE` already exists (`strings.txt:1223`) and is used in `basic.html:6` as the heading for the initial no-accounts setup guide. Plan 03's collapsible Client-ID wizard summary MUST use the new key `PLUGIN_SPOTON_CLIENT_ID_WIZARD_TITLE`, NOT the existing setup guide title key.

## Review finding: SETUP_DEVAPP_DESC dual-use

`PLUGIN_SPOTON_SETUP_DEVAPP_DESC` is used in two locations: the planned Client-ID wizard (`basic.html` Client-ID WRAPPER block) AND the existing no-accounts setup guide (`basic.html:18`). The rewritten content must use position-neutral language — avoid spatial references like "paste it into the field above" or "the redirect URI shown below" since those only make sense in one of the two rendering contexts. Instead use explicit naming like "paste it into the Client ID field in SpotOn Settings".

<tasks>

<task type="auto">
  <name>Task 1: Rename _doFlavouredRequest to _doRequest in Client.pm (D-14)</name>
  <files>Plugins/SpotOn/API/Client.pm</files>
  <read_first>
    - Plugins/SpotOn/API/Client.pm (full file — locate all occurrences of _doFlavouredRequest: sub definition at line 1069, call site at line 1057, dispatch comment at line 1017, doc-comment block at lines 1065-1068, and 4 stale endpoint comments at lines 245, 259, 292, 306)
    - .planning/phases/53-keymaster-removal-migration/53-PATTERNS.md (Client.pm pattern assignment — exact lines and stale doc-comment content)
  </read_first>
  <action>
    Per D-14 (rename + stale comment cleanup, no behavioral change):

    1. Rename the sub definition at line 1069 from `sub _doFlavouredRequest` to `sub _doRequest`.

    2. Update the call site at line 1057: `$class->_doFlavouredRequest(...)` to `$class->_doRequest(...)`.

    3. Update the dispatch comment at line 1017: change "_doFlavouredRequest" to "_doRequest" in the numbered step list.

    4. Rewrite the doc-comment block above the sub (lines 1065-1068). The current text describes a "bundled fallback on 403/410/deprecated-404" and "recursively for the bundled retry (isRetry=1)" — neither mechanism exists anymore (Phase 50 D-04 deleted the retry logic). Rewrite to describe what the function actually does today: execute a single PKCE-token HTTP request, handle response, normalize errors, invoke user callback.

    5. Update the 4 stale endpoint comments at lines 245, 259, 292, 306: replace "_doFlavouredRequest" with "_doRequest" in each.

    Do NOT change any function parameters, logic, or behavior. This is a pure naming/comment cleanup.
    Verify no `isRetry` parameter or `bundled` variable reference remains in the function body (they should already be gone from Phase 50 — confirm).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/08_api_client.t 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_doFlavouredRequest" Plugins/SpotOn/API/Client.pm` returns 0
    - `grep -c "_doRequest" Plugins/SpotOn/API/Client.pm` returns at least 3 (sub definition, call site, dispatch comment)
    - `grep -ci "bundled fallback\|bundled retry\|isRetry" Plugins/SpotOn/API/Client.pm` returns 0 in the _doRequest doc-comment block
    - `prove t/08_api_client.t` passes (no behavioral change)
  </acceptance_criteria>
  <done>_doFlavouredRequest renamed to _doRequest everywhere in Client.pm. All stale Keymaster/flavor doc-comments rewritten. Tests pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Keymaster i18n strings + add migration/auth strings + regression test (D-15, D-01/D-02)</name>
  <files>Plugins/SpotOn/strings.txt, t/02_strings.t, .planning/REQUIREMENTS.md</files>
  <read_first>
    - Plugins/SpotOn/strings.txt (lines 729-740 for PLUGIN_SPOTON_CLIENT_ID_DESC, lines 1366-1377 for PLUGIN_SPOTON_SETUP_DEVAPP_DESC — these are the 22 strings to rewrite)
    - Plugins/SpotOn/strings.txt (lines 1220-1230 for the existing PLUGIN_SPOTON_SETUP_GUIDE_TITLE key — must NOT be duplicated)
    - Plugins/SpotOn/strings.txt (scan for existing key naming patterns and tab-delimited format — keys on their own line, translations indented with one tab + language code + tab + text)
    - t/02_strings.t (full file — understand existing test structure to add the keymaster regression assertion)
    - .planning/phases/53-keymaster-removal-migration/53-CONTEXT.md (D-15 scope: exactly these 2 keys x 11 languages; D-01/D-02: AUTH-06 dropped)
    - .planning/REQUIREMENTS.md (AUTH-06 line to annotate)
  </read_first>
  <action>
    Per D-15 (22 strings = 2 keys x 11 languages) and the new strings needed by Plan 03:

    PART A — Rewrite PLUGIN_SPOTON_CLIENT_ID_DESC (lines 729-740, 11 translations):
    Replace the current text that mentions Spotify rejecting newer Developer Apps via Keymaster with PKCE-context text describing that a Spotify Developer App Client ID is required for PKCE authentication. The Client ID enables direct OAuth login and dedicated API rate limits. Keep the format: tab-indented, same 11 language codes (CS, DA, DE, EN, ES, FR, IT, NL, NO, PL, SV).

    PART B — Rewrite PLUGIN_SPOTON_SETUP_DEVAPP_DESC (lines 1366-1377, 11 translations):
    Replace the current setup guide with a step-by-step PKCE setup wizard description:
    Step 1: Go to developer.spotify.com/dashboard and log in
    Step 2: Create a new app (any name)
    Step 3: In app settings, add the redirect URI (reference using position-neutral language: "Add the Redirect URI shown in the SpotOn Settings page")
    Step 4: Copy the Client ID and paste it into the Client ID field in SpotOn Settings
    CRITICAL: This key is rendered in TWO locations — (1) the new Client-ID wizard collapsible section AND (2) the existing no-accounts setup guide at basic.html:18. Use position-neutral wording throughout (no "the field above", "below", "this section" — reference locations by explicit name "Client ID field in SpotOn Settings" and "Redirect URI shown in the SpotOn Settings page"). Addresses review concern: SETUP_DEVAPP_DESC dual-use.

    PART C — Add NEW i18n keys (these are beyond D-15's 22, for Plan 03 consumption):

    1. PLUGIN_SPOTON_MIGRATION_REQUIRED — "Please reconnect your Spotify account in Settings to enable Browse, Search, and Library. Connect playback continues to work." (migration hint for OPML menu, D-04)

    2. PLUGIN_SPOTON_AUTH_REQUIRED — "Authentication required. Please check your Spotify account in SpotOn Settings." (shown in feed error branches instead of generic "no results" when auth is the cause, D-04)

    3. PLUGIN_SPOTON_MIGRATION_SETTINGS_HINT — "Your Spotify account needs to be reconnected for Browse and Library access. Click 'Connect Spotify Account' below." (Settings page migration banner, D-04 Channel 2)

    4. PLUGIN_SPOTON_CLIENT_ID_WIZARD_TITLE — "How to set up your Spotify Developer App" (collapsible summary title for the Client-ID setup wizard, D-12). Addresses review concern: this is a NEW key. Do NOT reuse the existing PLUGIN_SPOTON_SETUP_GUIDE_TITLE (which already exists at strings.txt:1223 for the no-accounts initial setup guide heading and is rendered in basic.html:6).

    Each new key needs all 11 language translations (CS, DA, DE, EN, ES, FR, IT, NL, NO, PL, SV) in the standard tab-delimited format. Add them near the existing SPOTON auth/settings string block.

    PART D — AUTH-06 disposition (D-01/D-02):
    In .planning/REQUIREMENTS.md, update the AUTH-06 line to mark it as dropped:
    Change `- [ ] **AUTH-06**:` to `- [x] **AUTH-06**: ~~Login5 fallback~~ (dropped — Login5 gets immediate 429 on api.spotify.com, not viable as API token source. PKCE is the sole API auth path. See Phase 53 D-01/D-02)`
    Also update the Traceability table if AUTH-06 appears there.

    PART E — Permanent keymaster regression assertion in t/02_strings.t:
    Addresses review concern: no permanent regression tests for Keymaster removal. Add a test that greps strings.txt for case-insensitive references and asserts zero hits. This ensures a future commit cannot reintroduce the term without a test failing. Use the existing test structure and add the assertion as a new subtest after the existing structural checks. The assertion should grep through the translation values (excluding comments), not key names.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/02_strings.t 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "keymaster" Plugins/SpotOn/strings.txt` returns 0
    - `grep -c "PLUGIN_SPOTON_MIGRATION_REQUIRED" Plugins/SpotOn/strings.txt` returns at least 1 (key line)
    - `grep -c "PLUGIN_SPOTON_AUTH_REQUIRED" Plugins/SpotOn/strings.txt` returns at least 1 (key line)
    - `grep -c "PLUGIN_SPOTON_MIGRATION_SETTINGS_HINT" Plugins/SpotOn/strings.txt` returns at least 1 (key line)
    - `grep -c "PLUGIN_SPOTON_CLIENT_ID_WIZARD_TITLE" Plugins/SpotOn/strings.txt` returns at least 1 (key line — the NEW key)
    - `grep -c "PLUGIN_SPOTON_SETUP_GUIDE_TITLE" Plugins/SpotOn/strings.txt` returns exactly 1 (the existing key, unchanged)
    - Each of the 4 new keys has exactly 11 translation lines (CS through SV)
    - `grep "AUTH-06" .planning/REQUIREMENTS.md` shows "dropped" annotation
    - `prove t/02_strings.t` passes (including the new keymaster regression assertion)
  </acceptance_criteria>
  <done>All 22 existing Keymaster strings rewritten for PKCE context with position-neutral wording. 4 new i18n keys added with 11 languages each (using PLUGIN_SPOTON_CLIENT_ID_WIZARD_TITLE, not the existing SETUP_GUIDE_TITLE). Permanent keymaster regression test added to t/02_strings.t. AUTH-06 marked as dropped in REQUIREMENTS.md. String structure tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n strings -> user-facing UI | Rewritten strings are rendered in OPML menus and Settings pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-03 | Information Disclosure | i18n strings | low | accept | New strings use fixed, pre-translated text only. No dynamic interpolation of tokens, errors, or account IDs. Follows the T-51-10/T-52-02 pattern. |
| T-53-04 | Tampering | _doRequest rename | low | accept | Pure cosmetic rename, no behavioral change. Same function, same parameters, same call site. |
</threat_model>

<verification>
- No Keymaster references remain in strings.txt (verified by both one-time grep and permanent regression test in t/02_strings.t)
- Client.pm has no _doFlavouredRequest references
- All new string keys are properly structured (key line + 11 translations)
- PLUGIN_SPOTON_CLIENT_ID_WIZARD_TITLE is a new key distinct from existing PLUGIN_SPOTON_SETUP_GUIDE_TITLE
- PLUGIN_SPOTON_SETUP_DEVAPP_DESC uses position-neutral wording (no spatial references)
- t/02_strings.t and t/08_api_client.t both pass
</verification>

<success_criteria>
Client.pm uses _doRequest consistently. strings.txt has zero Keymaster mentions and contains all 4 new i18n keys needed by Plan 03 (using PLUGIN_SPOTON_CLIENT_ID_WIZARD_TITLE for the wizard, not the existing setup guide title). SETUP_DEVAPP_DESC rewrite works in both rendering contexts. A permanent regression test prevents keymaster reintroduction. AUTH-06 is documented as dropped.
</success_criteria>

<output>
Create `.planning/phases/53-keymaster-removal-migration/53-02-SUMMARY.md` when done
</output>
