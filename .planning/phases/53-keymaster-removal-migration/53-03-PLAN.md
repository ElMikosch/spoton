---
phase: 53-keymaster-removal-migration
plan: 03
type: execute
wave: 2
depends_on: [53-02]
files_modified:
  - Plugins/SpotOn/API/TokenManager.pm
  - t/07_token_manager.t
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
autonomous: true
requirements: [AUTH-07]
must_haves:
  truths:
    - "v2.x migration accounts (credentials.json without pkce_tokens.json) are detected proactively at OPML menu render time, not reactively after a failed API call"
    - "OPML menu shows a 'please reconnect' migration hint before any Browse/Library attempt is needed"
    - "Browse/Library/Search feed functions show 'authentication required' instead of generic 'no results' when auth is the cause"
    - "Settings page shows a migration banner when the active account needs migration"
    - "Client-ID section has a collapsible PKCE setup wizard with the redirect URI from the PKCE constant"
    - "Migration hint auto-dismisses once the user completes PKCE auth (pkce_tokens.json exists)"
    - "TokenManager.pm has zero Keymaster references (D-16)"
  artifacts:
    - "TokenManager.pm::accountNeedsMigration($accountId) helper"
    - "Plugin.pm::_authRequiredItem($client, $accountId, $err) shared helper"
    - "Settings.pm needsMigration + redirectUri template parameters"
    - "basic.html migration banner + Client-ID setup wizard collapsible section"
  key_links:
    - "accountNeedsMigration depends on Credentials::credentialsPathFor and PKCE::loadTokens — both already exist"
    - "_authRequiredItem depends on the PLUGIN_SPOTON_AUTH_REQUIRED string key from Plan 02"
    - "handleFeed migration hint depends on the PLUGIN_SPOTON_MIGRATION_REQUIRED string key from Plan 02"
    - "basic.html migration banner depends on the PLUGIN_SPOTON_MIGRATION_SETTINGS_HINT string key from Plan 02"
    - "basic.html setup wizard depends on the PLUGIN_SPOTON_SETUP_GUIDE_TITLE and rewritten PLUGIN_SPOTON_SETUP_DEVAPP_DESC from Plan 02"
---

<objective>
Migration detection + all UX wiring: new accountNeedsMigration helper (D-05), OPML migration hint (D-04 Channel 1), _authRequiredItem shared helper for 17 feed error branches (D-04), Settings migration banner (D-04 Channel 2), Client-ID PKCE setup wizard (D-11/D-12/D-13), TokenManager stale comment fix (D-16), auto-dismiss (D-06).

Purpose: Complete the migration UX for existing v2.x ZeroConf users upgrading to v3.0 PKCE. Proactive detection and guidance at every touchpoint — OPML menu, Browse/Library feeds, Settings page — without requiring users to discover the problem through failed API calls.

Output: Working migration detection and guidance across all 4 channels (OPML, Settings, feed errors, log).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-keymaster-removal-migration/53-CONTEXT.md
@.planning/phases/53-keymaster-removal-migration/53-RESEARCH.md
@.planning/phases/53-keymaster-removal-migration/53-PATTERNS.md
@.planning/phases/50-perl-tokenmanager-rewrite/50-01-SUMMARY.md
@.planning/phases/51-credential-derivation-connect/51-01-SUMMARY.md
</context>

## Artifacts this phase produces

Plan 03 artifacts:
- `TokenManager::accountNeedsMigration($accountId)` — public class method, returns boolean
- `TokenManager::anyAccountNeedsMigration()` — public class method, iterates all accounts, returns boolean (convenience for Settings.pm)
- `Plugin.pm::_authRequiredItem($client, $accountId, $err)` — private helper, returns OPML item hashref
- `Settings.pm::handler()` additions: `$paramRef->{needsMigration}`, `$paramRef->{redirectUri}`
- `basic.html` migration banner (conditional on needsMigration template var)
- `basic.html` Client-ID setup wizard collapsible section (PLUGIN_SPOTON_SETUP_GUIDE_TITLE summary, PLUGIN_SPOTON_SETUP_DEVAPP_DESC body, redirectUri)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration detection helper + unit tests + TokenManager comment fix (D-05, D-16)</name>
  <files>Plugins/SpotOn/API/TokenManager.pm, t/07_token_manager.t</files>
  <read_first>
    - Plugins/SpotOn/API/TokenManager.pm (full file — understand the public API: getToken, needsReauth, anyAccountNeedsReauth, getAccountIds, clearNeedsReauth; find line 362 stale comment)
    - Plugins/SpotOn/API/Credentials.pm (credentialsPathFor method — lines 198-204, _accountDir at lines 323-326)
    - Plugins/SpotOn/API/PKCE.pm (loadTokens function — lines 304-324, returns undef on absence)
    - Plugins/SpotOn/Settings.pm (lines 658-666 — _isPkceConfigured pattern to mirror for the per-account variant)
    - t/07_token_manager.t (full file — understand the write_stub pattern, existing PKCE stub with loadTokens_by_account, existing $cache_dir temp directory, prefs stub with cachedir => $cache_dir)
    - .planning/phases/53-keymaster-removal-migration/53-PATTERNS.md (TokenManager.pm pattern assignments — exact helper shape)
  </read_first>
  <behavior>
    - Test A: account with credentials.json but no pkce_tokens.json returns accountNeedsMigration true
    - Test B: account with credentials.json AND pkce_tokens.json returns accountNeedsMigration false (auto-dismiss, D-06)
    - Test C: account with no credentials.json returns accountNeedsMigration false (not a v2.x user)
    - Test D: anyAccountNeedsMigration returns 1 when at least one account needs migration
    - Test E: anyAccountNeedsMigration returns 0 when no account needs migration
  </behavior>
  <action>
    Per D-05 (migration detection formula: credentials.json exists AND pkce_tokens.json absent) and D-16 (remove last Keymaster comment reference):

    PART A — Credentials stub in t/07_token_manager.t (RED phase prerequisite):
    Add a Credentials stub using the existing write_stub pattern, placed near the other Plugins::SpotOn stubs. The stub needs only credentialsPathFor, which returns catfile($cache_dir, 'spoton', $accountId, 'credentials.json') — mirroring the real _accountDir logic using the same $cache_dir that the prefs stub already provides as cachedir. Use File::Spec::Functions catfile/catdir (already imported in the test file at line 727).

    PART B — Unit tests in t/07_token_manager.t (RED phase):
    Add a new test section after the existing anyAccountNeedsReauth tests (after the last SKIP block test, before done_testing). Five subtests per the behavior block above:
    (A) Create a fake account dir under $cache_dir/spoton/mig_needs/ with only credentials.json, set PKCE loadTokens_by_account for mig_needs to undef. Assert accountNeedsMigration returns 1.
    (B) Same dir but also set PKCE loadTokens_by_account for mig_has_pkce to a truthy hashref, create credentials.json in its dir. Assert accountNeedsMigration returns 0.
    (C) Do NOT create any dir/file for mig_no_creds. Assert accountNeedsMigration returns 0.
    (D) Set up prefs with two accounts: one needs migration (credentials.json only), one has PKCE. Assert anyAccountNeedsMigration returns 1.
    (E) Set up prefs with two accounts both having PKCE tokens. Assert anyAccountNeedsMigration returns 0.

    PART C — New accountNeedsMigration method (GREEN phase):
    Add a public class method accountNeedsMigration($class, $accountId) to TokenManager.pm. Implementation per D-05 and the PATTERNS.md suggested shape:
    - require Plugins::SpotOn::API::Credentials and Plugins::SpotOn::API::PKCE
    - return 0 unless -f Plugins::SpotOn::API::Credentials->credentialsPathFor($accountId)
    - return PKCE::loadTokens($accountId) ? 0 : 1

    Do NOT use Credentials->verifyCredentials() — the simpler file-existence check per D-05's literal formula is correct (a credentials.json that exists but has non-standard auth_type still indicates a v2.x user who needs migration).

    PART D — New anyAccountNeedsMigration convenience method (GREEN phase):
    Add a public class method anyAccountNeedsMigration($class) that iterates getAccountIds() and returns 1 if any account returns true from accountNeedsMigration. Same pattern as the existing anyAccountNeedsReauth().

    PART E — Fix stale comment at line 362 (D-16):
    The comment currently says "instead of the deleted _fetchKeymasterToken" — remove that clause. The surrounding doc-comment for _fetchDisplayName should describe the current behavior without referencing the deleted function. Verify via grep that no other case-insensitive reference remains in TokenManager.pm after this fix.

    Place the new methods near the existing needsReauth/anyAccountNeedsReauth methods for logical grouping.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/07_token_manager.t 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sub accountNeedsMigration" Plugins/SpotOn/API/TokenManager.pm` returns 1
    - `grep -c "sub anyAccountNeedsMigration" Plugins/SpotOn/API/TokenManager.pm` returns 1
    - `grep -ci "keymaster" Plugins/SpotOn/API/TokenManager.pm` returns 0
    - `grep -c "credentialsPathFor" Plugins/SpotOn/API/TokenManager.pm` returns at least 1 (inside accountNeedsMigration)
    - `grep -c "PKCE::loadTokens" Plugins/SpotOn/API/TokenManager.pm` returns at least 1 (inside accountNeedsMigration)
    - `grep -c "accountNeedsMigration" t/07_token_manager.t` returns at least 5 (test assertions)
    - `prove t/07_token_manager.t` passes with all migration detection subtests green
  </acceptance_criteria>
  <done>TokenManager has accountNeedsMigration and anyAccountNeedsMigration public methods with dedicated unit tests covering migration-needed, already-migrated, no-credentials, and aggregate detection. Zero Keymaster references remain in the file.</done>
</task>

<task type="auto">
  <name>Task 2: OPML migration hint + _authRequiredItem helper + 17 feed site wiring (D-04)</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (lines 418-435 — handleFeed existing re-auth + rate-limit hint blocks as pattern templates)
    - Plugins/SpotOn/Plugin.pm (lines 1210-1230 — _recentlyPlayedFeed as the template for the callback-signature change at all 17 sites)
    - Plugins/SpotOn/Plugin.pm (grep for "sub _" to locate all feed functions and the _xxxItem helper functions nearby — _authRequiredItem should be placed among the existing helpers)
    - .planning/phases/53-keymaster-removal-migration/53-PATTERNS.md (Plugin.pm OPML migration hint pattern, 17x unless($data) pattern, _authRequiredItem helper shape)
    - .planning/phases/53-keymaster-removal-migration/53-RESEARCH.md (Pitfall 2 — do not underscope the 17 sites; Pitfall 3 — do not gate on needsReauth alone)
  </read_first>
  <action>
    Per D-04 (proactive migration hint, auth-required error messages) and D-06 (auto-dismiss via D-05 formula):

    PART A — OPML migration hint in handleFeed() (D-04 Channel 1):
    After the existing anyAccountNeedsReauth() block (lines 420-427), add a new independent migration check per the PATTERNS.md template:
    - Get the active accountId via _getAccountId($client)
    - Call Plugins::SpotOn::API::TokenManager->accountNeedsMigration($accountId)
    - If true, push a textarea item with the PLUGIN_SPOTON_MIGRATION_REQUIRED string (from Plan 02)
    - Per RESEARCH.md Open Question 1 recommendation: apply precedence — show migration hint if true; only show the existing reauth hint if migration hint is false (they are normally mutually exclusive, but this avoids ever double-stacking)

    PART B — New _authRequiredItem shared helper:
    Add a private sub `_authRequiredItem` in Plugin.pm near the other _xxxItem helpers. Shape per PATTERNS.md:
    - Parameters: ($client, $accountId, $err)
    - Check: accountNeedsMigration($accountId) OR needsReauth($accountId)
    - If true: return hashref with name => cstring($client, 'PLUGIN_SPOTON_AUTH_REQUIRED'), type => 'textarea'
    - If false: return hashref with name => cstring($client, 'PLUGIN_SPOTON_NO_RESULTS'), type => 'textarea'
    - SECURITY (T-51-10/T-52-02 pattern): NEVER interpolate $err into the returned name — fixed, pre-translated cstring() keys only. The $err parameter is reserved for future server-side logging if needed but is not reflected into user-facing text.

    PART C — Wire _authRequiredItem into all 17 unless($data) sites:
    At each of the 17 call sites (lines 1222, 1324, 1429, 1455, 1491, 1544, 1584, 1641, 1742, 2065, 2121, 2173, 2260, 2360, 2442, 2515, 2726):
    1. Change the callback signature from `my $data = shift;` to `my ($data, $err) = @_;`
    2. Replace the inline `{ name => cstring($client, 'PLUGIN_SPOTON_NO_RESULTS'), type => 'textarea' }` item construction inside the `unless ($data)` block with a call to `_authRequiredItem($client, $accountId, $err)`
    3. Confirm $accountId is already in scope at each site (it is — every feed function resolves it via _getAccountId($client) near the top)

    This is a mechanical substitution repeated 17 times. The pattern is identical at every site.

    D-06 auto-dismiss requires no code: once the user completes PKCE auth, pkce_tokens.json exists, and accountNeedsMigration() returns false on the next render.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/ 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accountNeedsMigration" Plugins/SpotOn/Plugin.pm` returns at least 2 (handleFeed check + _authRequiredItem check)
    - `grep -c "_authRequiredItem" Plugins/SpotOn/Plugin.pm` returns at least 18 (1 sub definition + 17 call sites)
    - `grep -c 'my \$data = shift' Plugins/SpotOn/Plugin.pm` returns 0 inside feed function callbacks (all changed to `my ($data, $err) = @_;`)
    - `grep -c 'PLUGIN_SPOTON_MIGRATION_REQUIRED' Plugins/SpotOn/Plugin.pm` returns at least 1 (handleFeed hint)
    - `grep -c 'PLUGIN_SPOTON_AUTH_REQUIRED' Plugins/SpotOn/Plugin.pm` returns at least 1 (inside _authRequiredItem)
    - No raw $err value is interpolated into any cstring() call or user-facing string
    - `prove t/` passes (full suite)
  </acceptance_criteria>
  <done>handleFeed shows proactive migration hint. All 17 feed error branches use the shared _authRequiredItem helper. Auth-related empty states show "authentication required" instead of generic "no results". Full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Settings migration banner + Client-ID PKCE setup wizard (D-04 Ch.2, D-11/D-12/D-13)</name>
  <files>Plugins/SpotOn/Settings.pm, Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</files>
  <read_first>
    - Plugins/SpotOn/Settings.pm (lines 255-270 — handler() $paramRef wiring for degradedMode/pkceConfigured/spDcStatus, the pattern to follow)
    - Plugins/SpotOn/Settings.pm (lines 658-666 — _isPkceConfigured, to understand the existing migration-related helper)
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (lines 1-30 — existing setup guide block for accounts.keys.size == 0)
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (lines 82-94 — existing Client-ID WRAPPER block where the wizard goes)
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (lines 161-164 — sp_dc collapsible how-to pattern to replicate for D-12)
    - Plugins/SpotOn/API/PKCE.pm (line 39 — GITHUB_PAGES_REDIRECT_URI constant, single source of truth for D-13)
    - .planning/phases/53-keymaster-removal-migration/53-PATTERNS.md (Settings.pm pattern assignment, basic.html setup wizard pattern)
  </read_first>
  <action>
    Per D-04 (Channel 2 — Settings migration banner), D-11 (Client-ID stays), D-12 (collapsible setup wizard), D-13 (callback URI from constant):

    PART A — Settings.pm handler() additions:
    In the handler() method, after the existing $paramRef->{pkceConfigured} line:
    1. Add `$paramRef->{needsMigration} = Plugins::SpotOn::API::TokenManager->anyAccountNeedsMigration();`
       (require TokenManager if not already required in this scope)
    2. Add `$paramRef->{redirectUri} = Plugins::SpotOn::API::PKCE::GITHUB_PAGES_REDIRECT_URI();`
       (fully-qualified call, no import needed for a use constant sub — per Pitfall 4, do NOT hardcode a second copy of the URI string)

    PART B — basic.html migration banner (D-04 Channel 2):
    Add a conditional migration banner near the top of the settings page (after the existing degradedMode warning block). Pattern follows the existing degradedMode orange warning style:
    - `[% IF needsMigration %]` block
    - Orange/amber banner text using the PLUGIN_SPOTON_MIGRATION_SETTINGS_HINT string key (from Plan 02)
    - Link to the "Connect Spotify Account" button section below (or just a text instruction pointing to the PKCE auth button)
    - `[% END %]`

    PART C — Client-ID setup wizard (D-12/D-13):
    Inside the existing Client-ID WRAPPER block (lines 82-94 of basic.html), after the input field:
    1. Add a `<details style="margin-top:10px">` block mirroring the sp_dc how-to at lines 161-164
    2. Summary: `[% 'PLUGIN_SPOTON_SETUP_GUIDE_TITLE' | string %]` (from Plan 02)
    3. Body paragraph: `[% 'PLUGIN_SPOTON_SETUP_DEVAPP_DESC' | string %]` (rewritten in Plan 02 to contain the step-by-step wizard)
    4. Show the redirect URI via the template variable: `[% redirectUri | html %]` in a copyable format (e.g., inside a code/monospace styled span or a readonly input)
    5. Do NOT hardcode the URI string — use only the template variable populated from the PKCE constant in Settings.pm

    The existing Client-ID input field, placeholder, and degradedMode warning all stay unchanged (D-11).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/ 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "needsMigration" Plugins/SpotOn/Settings.pm` returns at least 1
    - `grep -c "redirectUri" Plugins/SpotOn/Settings.pm` returns at least 1
    - `grep -c "GITHUB_PAGES_REDIRECT_URI" Plugins/SpotOn/Settings.pm` returns 1 (fully-qualified constant call)
    - `grep -c "stiefenm.github.io" Plugins/SpotOn/Settings.pm` returns 0 (no hardcoded duplicate per Pitfall 4)
    - `grep -c "needsMigration" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` returns at least 1
    - `grep -c "redirectUri" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` returns at least 1
    - `grep -c "PLUGIN_SPOTON_SETUP_GUIDE_TITLE" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` returns at least 1
    - `grep -c "PLUGIN_SPOTON_MIGRATION_SETTINGS_HINT" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` returns at least 1
    - The existing Client-ID input field and degradedMode warning remain unchanged
    - `prove t/` passes
  </acceptance_criteria>
  <done>Settings page shows migration banner for v2.x accounts. Client-ID section has a collapsible PKCE setup wizard with the redirect URI from the PKCE constant. Full test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client.pm error callback → Plugin.pm OPML | $err from API responses crosses into the OPML rendering layer via _authRequiredItem |
| Settings.pm → basic.html template | $paramRef values are interpolated into user-facing HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-05 | Information Disclosure | _authRequiredItem | medium | mitigate | $err parameter is accepted but NEVER interpolated into cstring() output. Only fixed, pre-translated string keys (PLUGIN_SPOTON_AUTH_REQUIRED or PLUGIN_SPOTON_NO_RESULTS) are returned. Follows Phase 51 T-51-10 / Phase 52 T-52-02 pattern. |
| T-53-06 | Information Disclosure | redirectUri template var | low | mitigate | The redirect URI is a public GitHub Pages URL (not a secret). It is HTML-escaped via the `| html` TT2 filter in the template. No injection vector. |
| T-53-07 | Spoofing | accountNeedsMigration | low | accept | Pure filesystem read (file existence check) with no network call. Cannot be spoofed remotely. A local attacker with filesystem access could create/delete files, but that is out-of-scope for ASVS Level 1. |
</threat_model>

<verification>
- accountNeedsMigration correctly detects v2.x accounts (credentials.json present, pkce_tokens.json absent)
- accountNeedsMigration returns false after PKCE auth completes (auto-dismiss, D-06)
- OPML menu shows migration hint BEFORE any Browse/Library attempt
- Feed functions show auth-required message instead of generic no-results when auth is the cause
- Settings page shows migration banner when applicable
- Client-ID setup wizard is collapsible and shows the correct redirect URI
- No raw error details leak into user-facing text
- Full test suite passes
</verification>

<success_criteria>
The complete migration UX is wired: proactive OPML hint, auth-required feed errors, Settings banner, and PKCE setup wizard. Auto-dismiss works via the filesystem-based detection formula. TokenManager has zero Keymaster references. No information disclosure in error paths.
</success_criteria>

<output>
Create `.planning/phases/53-keymaster-removal-migration/53-03-SUMMARY.md` when done
</output>
