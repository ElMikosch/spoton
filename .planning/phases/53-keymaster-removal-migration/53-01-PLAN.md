---
phase: 53-keymaster-removal-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - librespot-spoton/src/main.rs
  - librespot-spoton/src/unified.rs
autonomous: true
requirements: [AUTH-07]
must_haves:
  truths:
    - "The spoton binary no longer accepts --get-token or --scope or --client-id as CLI flags"
    - "The spoton binary no longer contains the run_get_token() function or Mode::GetToken variant"
    - "The scope and client_id variables are removed (no orphaned dead code)"
    - "KEYMASTER_CLIENT_ID is renamed to DISCOVERY_CLIENT_ID in both main.rs and unified.rs with the same literal value"
    - "cargo check succeeds with zero warnings about unused variables"
  artifacts:
    - "librespot-spoton/src/main.rs without Mode::GetToken, run_get_token(), --get-token/--client-id/--scope parsing, scope/client_id variables"
    - "librespot-spoton/src/unified.rs with DISCOVERY_CLIENT_ID"
  key_links:
    - "Discovery::builder() calls use the renamed DISCOVERY_CLIENT_ID constant — ZeroConf mDNS registration remains functional"
---

<objective>
Remove all Keymaster-calling Rust code from the librespot-spoton binary: delete Mode::GetToken, run_get_token(), --get-token/--client-id/--scope CLI flag parsing, and the scope/client_id variables. Rename KEYMASTER_CLIENT_ID to DISCOVERY_CLIENT_ID in both main.rs and unified.rs.

Purpose: AUTH-07 completion (Rust layer) — eliminates the only remaining code that would call hm://keymaster/token/authenticated. After this plan, the binary physically cannot make a Keymaster service call.

Output: Cleaned Rust source that compiles successfully with cargo check and zero unused-variable warnings.

Review concern addressed: --scope flag and scope variable removal (review finding #2 — these exist solely for the GetToken path and would be orphaned dead code after Mode::GetToken deletion).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-keymaster-removal-migration/53-CONTEXT.md
@.planning/phases/53-keymaster-removal-migration/53-RESEARCH.md
@.planning/phases/53-keymaster-removal-migration/53-PATTERNS.md
</context>

## Artifacts this phase produces

Plan 01 artifacts (deletions and renames only — no new symbols):
- `DISCOVERY_CLIENT_ID` constant in `main.rs:run_discover_once()` (renamed from `KEYMASTER_CLIENT_ID`)
- `DISCOVERY_CLIENT_ID` constant in `unified.rs` reconnect-Discovery branch (renamed from `KEYMASTER_CLIENT_ID`)
- Deleted: `Mode::GetToken` enum variant, `run_get_token()` async fn, `--get-token` flag arm, `--client-id` flag arm, `--scope` flag arm, `client_id` variable, `scope` variable

<tasks>

<task type="auto">
  <name>Task 1: Remove Mode::GetToken, run_get_token(), and all GetToken-only CLI flags from main.rs (D-07)</name>
  <files>librespot-spoton/src/main.rs</files>
  <read_first>
    - librespot-spoton/src/main.rs (full file — understand the Mode enum, flag parsing loop, match arms, and run_get_token function)
    - .planning/phases/53-keymaster-removal-migration/53-PATTERNS.md (Pattern Assignment for main.rs — exact lines to delete)
    - .planning/phases/53-keymaster-removal-migration/53-RESEARCH.md (Pitfall 5 — confirm --client-id has no other consumer)
  </read_first>
  <action>
    Per D-07 (clean cut, no deprecation wrapper) and D-10 (no binary version gate):

    1. Delete the doc-comment block for the --get-token contract: line 11 mentioning that flag in the mode list, and lines 25-28 describing the contract for TokenManager.pm. Keep other doc-comment lines intact.

    2. Delete the Mode::GetToken variant from the enum Mode definition at line 64.

    3. Delete the --get-token flag parsing arm at lines 131-133 inside the args loop.

    4. Delete the --scope flag parsing arm at lines 245-249 (the entire match arm including the if-block and i increment). Addresses review concern: --scope variable and flag exist solely for the GetToken path and would be orphaned dead code after Mode::GetToken deletion.

    5. Delete the --client-id flag parsing arm at lines 251-256 (the entire match arm including the if-block, clone, i increment, and closing braces). Confirmed: client_id variable's only consumers are Mode::GetToken code — Pitfall 5 verified, safe to remove.

    6. Delete the scope variable declaration at line 98 (`let mut scope = String::new();`). This variable is only consumed by run_get_token (lines 520, 530) which is being deleted.

    7. Delete the client_id variable declaration at line 99 (`let mut client_id = String::new();`).

    8. Delete the Mode::GetToken match arm at lines 328-344 (cache_dir validation, run_get_token call, error handling, process::exit).

    9. Delete the entire run_get_token async function at lines 494-561 (session setup, token_provider().get_token() / get_token_with_client_id(), JSON output, env var write).

    Do NOT touch Mode::TokenLogin, Mode::Authenticate, Mode::DiscoverOnce, or Mode::Unified — these are surviving modes.
    Do NOT touch run_login(), run_token_login(), run_discover_once(), or run_unified() — these are unrelated functions.
  </action>
  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Mode::GetToken" librespot-spoton/src/main.rs` returns 0
    - `grep -c "run_get_token" librespot-spoton/src/main.rs` returns 0
    - `grep -c '"--get-token"' librespot-spoton/src/main.rs` returns 0
    - `grep -c '"--client-id"' librespot-spoton/src/main.rs` returns 0
    - `grep -c '"--scope"' librespot-spoton/src/main.rs` returns 0
    - `grep -c "let mut client_id" librespot-spoton/src/main.rs` returns 0
    - `grep -c "let mut scope" librespot-spoton/src/main.rs` returns 0
    - `grep -c "token_provider" librespot-spoton/src/main.rs` returns 0
    - Mode::Check, Mode::Authenticate, Mode::TokenLogin, Mode::DiscoverOnce, Mode::Unified all remain
    - `cargo check` succeeds with no errors
    - `cargo check 2>&1 | grep -c "warning: unused"` returns 0 (no orphaned variables)
  </acceptance_criteria>
  <done>main.rs compiles without Mode::GetToken, run_get_token(), --get-token, --client-id, --scope, or any token_provider reference. All surviving modes untouched. No unused variable warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Rename KEYMASTER_CLIENT_ID to DISCOVERY_CLIENT_ID in main.rs and unified.rs (D-08)</name>
  <files>librespot-spoton/src/main.rs, librespot-spoton/src/unified.rs</files>
  <read_first>
    - librespot-spoton/src/main.rs (lines 580-595 — run_discover_once, the KEYMASTER_CLIENT_ID const and its Discovery::builder usage)
    - librespot-spoton/src/unified.rs (lines 1548-1580 — reconnect Discovery branch, the second KEYMASTER_CLIENT_ID const)
    - .planning/phases/53-keymaster-removal-migration/53-PATTERNS.md (Rename pattern section)
  </read_first>
  <action>
    Per D-08 (cosmetic rename, same value, same functional purpose — ZeroConf mDNS discovery identity):

    In main.rs (inside run_discover_once()):
    1. Rename the const declaration: KEYMASTER_CLIENT_ID to DISCOVERY_CLIENT_ID (same value string "65b708073fc0480ea92a077233ca87bd")
    2. Update the comment above it from mentioning "KEYMASTER" to describing its actual purpose (mDNS Discovery identity, sourced from librespot-core default)
    3. Update the Discovery::builder() call that references KEYMASTER_CLIENT_ID to use DISCOVERY_CLIENT_ID

    In unified.rs (inside the reconnect Discovery branch):
    1. Same const rename: KEYMASTER_CLIENT_ID to DISCOVERY_CLIENT_ID (same value)
    2. Update the Discovery::builder() call that references KEYMASTER_CLIENT_ID to use DISCOVERY_CLIENT_ID

    Do NOT change the value "65b708073fc0480ea92a077233ca87bd" — it is the standard librespot default client_id for mDNS.
    Do NOT touch any other code in unified.rs — the reconnect Discovery branch is the only place this const appears.
  </action>
  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "KEYMASTER_CLIENT_ID" librespot-spoton/src/main.rs` returns 0
    - `grep -c "KEYMASTER_CLIENT_ID" librespot-spoton/src/unified.rs` returns 0
    - `grep -c "DISCOVERY_CLIENT_ID" librespot-spoton/src/main.rs` returns at least 2 (const + usage)
    - `grep -c "DISCOVERY_CLIENT_ID" librespot-spoton/src/unified.rs` returns at least 2 (const + usage)
    - `grep "65b708073fc0480ea92a077233ca87bd" librespot-spoton/src/main.rs librespot-spoton/src/unified.rs` still returns 2 hits (value unchanged)
    - `cargo check` succeeds
  </acceptance_criteria>
  <done>Both Rust files use DISCOVERY_CLIENT_ID instead of KEYMASTER_CLIENT_ID. Same value, new name. cargo check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Binary CLI surface | Removing --get-token eliminates a code path that issued Keymaster/Mercury protocol calls to Spotify's AP servers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-01 | Spoofing | run_get_token deletion | low | mitigate | Complete removal of the Keymaster token-retrieval path eliminates the attack surface where a stale/compromised session could obtain tokens via Mercury protocol |
| T-53-02 | Tampering | DISCOVERY_CLIENT_ID rename | low | accept | Cosmetic rename only; same value, same functional use (ZeroConf mDNS identity). No behavioral change, no security impact. |
</threat_model>

<verification>
- `cargo check` succeeds after both tasks with zero warnings
- No "KEYMASTER" string remains in either Rust file
- No "get_token" or "GetToken" references remain in main.rs
- No orphaned `--scope`/`scope` or `--client-id`/`client_id` variables remain
- Surviving modes (Check, Authenticate, TokenLogin, DiscoverOnce, Unified) compile and match correctly
</verification>

<success_criteria>
The librespot-spoton binary source no longer contains any code that could call hm://keymaster/token/authenticated. The KEYMASTER_CLIENT_ID constant is renamed to DISCOVERY_CLIENT_ID in both files. All GetToken-only CLI flags and variables are removed with no orphaned dead code. cargo check passes with zero unused-variable warnings.
</success_criteria>

<output>
Create `.planning/phases/53-keymaster-removal-migration/53-01-SUMMARY.md` when done
</output>
