---
phase: 51-credential-derivation-connect
plan: 03
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/strings.txt
  - t/02_strings.t
  - t/09_settings.t
autonomous: true
requirements: [AUTH-03]
tags: [perl, oauth, pkce, settings-ui, i18n, lms-plugin]

must_haves:
  truths:
    - "Completing PKCE auth in Settings immediately derives credentials.json while the token is guaranteed fresh (D-01 eager call site)"
    - "On derivation success the success page renders and the daemon start is triggered unconditionally — the Connect device appears without waiting for the watchdog (D-06, Pitfall 6)"
    - "On derivation failure the user sees a warning ('Connect not available, please retry') but account creation still succeeds — Browse/Search keep working (D-02)"
    - "The success message fires only after derivation completes — OAuth success is no longer conflated with Connect-readiness"
  artifacts:
    - path: "Plugins/SpotOn/Settings.pm"
      provides: "Eager derivation call in _pkceStoreAccount's success callback"
      contains: "deriveCredentials"
    - path: "Plugins/SpotOn/strings.txt"
      provides: "PLUGIN_SPOTON_CONNECT_DERIVE_FAILED warning string, 11 languages"
      contains: "PLUGIN_SPOTON_CONNECT_DERIVE_FAILED"
  key_links:
    - from: "Plugins/SpotOn/Settings.pm"
      to: "Plugins/SpotOn/API/Credentials.pm"
      via: "deriveCredentials inside the _storeAccountPrefs success callback in _pkceStoreAccount"
      pattern: "Credentials->deriveCredentials"
    - from: "Plugins/SpotOn/Settings.pm"
      to: "Plugins/SpotOn/Unified/DaemonManager.pm"
      via: "unconditional scheduleInit on derivation success (D-06, not gated on first-account)"
      pattern: "DaemonManager->scheduleInit"
---

<objective>
Wire the Settings-eager derivation call site (D-01): immediately after PKCE auth completes and the account is stored, derive librespot credentials while the access token is guaranteed fresh, then trigger the daemon start unconditionally so the Connect device appears without delay (D-06 — explicitly NOT relying on _storeAccountPrefs's first-account-only scheduleInit conditional, per RESEARCH Pitfall 6 which documents that gap for "Add Another Account" and re-auth flows). User feedback follows D-02: transparent on success (the existing success string, now fired only after derivation completes — resolving RESEARCH Open Question 2), a new warning string on derivation failure ("Connect not available, please retry") that does NOT block account creation, since Browse/Search already work via PKCE tokens from Phase 50.

Purpose: AUTH-03 — the primary, user-facing trigger for credential derivation. Guarantees every fresh PKCE authentication produces Connect-ready credentials in one flow.
Output: Modified Settings.pm, new i18n key in strings.txt (11 languages), updated t/02_strings.t and t/09_settings.t.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/51-credential-derivation-connect/51-CONTEXT.md
@.planning/phases/51-credential-derivation-connect/51-RESEARCH.md
@.planning/phases/51-credential-derivation-connect/51-PATTERNS.md
@.planning/phases/51-credential-derivation-connect/51-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by this plan (source-grounding exclusion list):

- **i18n key:** `PLUGIN_SPOTON_CONNECT_DERIVE_FAILED` in strings.txt (11 languages: CS/DA/DE/EN/ES/FR/IT/NL/NO/PL/SV)
- **JSON response field:** `connectReady` (0/1) in _pkceStoreAccount's JSON success response
- **Wiring:** eager `Credentials->deriveCredentials` call + unconditional `DaemonManager->scheduleInit` inside Settings.pm `_pkceStoreAccount`
- **Test stubs:** `Plugins::SpotOn::API::Credentials` stub and DaemonManager scheduleInit recorder in t/09_settings.t

<tasks>

<task type="auto">
  <name>Task 1: Settings.pm — eager derivation in _pkceStoreAccount with D-02 feedback and D-06 daemon start</name>
  <files>Plugins/SpotOn/Settings.pm</files>
  <read_first>
    - Plugins/SpotOn/Settings.pm (_pkceStoreAccount at L473-520 — the exact insertion point: the _storeAccountPrefs success callback containing clearNeedsReauth and the isJson/page-render branch; _renderPkceResultPage signature at L591; _jsonResponse at L807)
    - Plugins/SpotOn/API/Credentials.pm (plan 51-01 output — deriveCredentials callback contract ($ok, $reason))
    - Plugins/SpotOn/API/TokenManager.pm (_storeAccountPrefs at L394-420 — note the $needsDaemonStart first-account-only conditional that Pitfall 6 documents; do not modify it)
    - .planning/phases/51-credential-derivation-connect/51-RESEARCH.md (Pitfall 6, Open Question 2 resolution)
  </read_first>
  <action>
    Inside _pkceStoreAccount's _storeAccountPrefs success callback (currently: INFO log → clearNeedsReauth → isJson/page render), replace the immediate response render with an eager derivation step (D-01 eager call site — the token was exchanged seconds ago, guaranteed fresh):

    - After clearNeedsReauth, `require Plugins::SpotOn::API::Credentials;` and call Plugins::SpotOn::API::Credentials->deriveCredentials($accountId, sub { my ($ok, $reason) = @_; ... }).
    - Success branch ($ok): unconditionally `require Plugins::SpotOn::Unified::DaemonManager;` and call Plugins::SpotOn::Unified::DaemonManager->scheduleInit() (D-06 — do NOT rely on _storeAccountPrefs's $needsDaemonStart conditional, which only fires for the very first account; Pitfall 6 documents that "Add Another Account" and re-auth flows would otherwise wait up to 60s for the watchdog). This also covers D-07: scheduleInit → initHelpers → startHelper, whose existing account-change detection restarts any daemon running with old credentials. Then render the existing success response: for isJson, _jsonResponse with { status => 'ok', accountId => $accountId, connectReady => 1 }; otherwise _renderPkceResultPage with string('PLUGIN_SPOTON_PKCE_SUCCESS') twice and isError 0 — the success message now fires only after derivation completes (Open Question 2 resolution: one unified user-facing signal per D-02, not a two-step narration).
    - Failure branch: WARN with masked accountId and $reason. Account creation is NOT rolled back — tokens are stored, Browse/Search work (Phase 50); this is a warning, not a failure (D-02). For isJson, _jsonResponse with { status => 'ok', accountId => $accountId, connectReady => 0, warning => string('PLUGIN_SPOTON_CONNECT_DERIVE_FAILED') }; otherwise _renderPkceResultPage with title string('PLUGIN_SPOTON_PKCE_SUCCESS') and message string('PLUGIN_SPOTON_CONNECT_DERIVE_FAILED'), isError 1 (renders the warning styling while making clear the account itself connected).
    - The derivation typically completes in <1s (spike-validated) with a 10s poll cap; the HTTP response is held open across the async callback — the LMS event loop is NOT blocked (Credentials.pm is timer-polled, Pitfall 2), only this one HTTP client waits.
    - Do not modify _storeAccountPrefs itself (TokenManager.pm is not in this plan's file set).
  </action>
  <verify>
    <automated>prove t/05_perl_syntax.t && prove t/09_settings.t</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'deriveCredentials' Plugins/SpotOn/Settings.pm` returns 1 (single eager call site inside _pkceStoreAccount)
    - `grep -c 'scheduleInit' Plugins/SpotOn/Settings.pm` — the count INCREASES by exactly 1 versus the pre-task file (the new unconditional D-06 call; an existing scheduleInit call site at L111 remains)
    - `grep -c 'PLUGIN_SPOTON_CONNECT_DERIVE_FAILED' Plugins/SpotOn/Settings.pm` returns >= 1 (failure branch renders the new string)
    - `grep -c 'connectReady' Plugins/SpotOn/Settings.pm` returns 2 (JSON success and failure branches)
    - The success page render (PLUGIN_SPOTON_PKCE_SUCCESS) is now inside the derivation callback — verified by t/09_settings.t structure assertions (Task 2)
    - `prove t/05_perl_syntax.t` green (Settings.pm compiles)
  </acceptance_criteria>
  <done>PKCE auth completion eagerly derives credentials, unconditionally triggers daemon start on success (Connect device appears without watchdog delay), and warns without blocking account creation on failure.</done>
</task>

<task type="auto">
  <name>Task 2: i18n string (11 languages) + test updates (t/02_strings.t, t/09_settings.t)</name>
  <files>Plugins/SpotOn/strings.txt, t/02_strings.t, t/09_settings.t</files>
  <read_first>
    - Plugins/SpotOn/strings.txt (PLUGIN_SPOTON_PKCE_SUCCESS block at L2263-2274 — the exact 11-language tab-indented format to replicate)
    - t/02_strings.t (@bilingual_keys list convention from Phase 50 Plan 03)
    - t/09_settings.t (stub harness: write_stub helper, existing TokenManager stub with anyAccountNeedsReauth/clearNeedsReauth, the "Settings.pm structure tests" grep-based block, and the _pkceStoreAccount-related tests that will now hit the new Credentials call)
  </read_first>
  <action>
    1. strings.txt: add key PLUGIN_SPOTON_CONNECT_DERIVE_FAILED directly after the PLUGIN_SPOTON_PKCE_ERROR block, same tab-indented format, all 11 languages (CS/DA/DE/EN/ES/FR/IT/NL/NO/PL/SV). EN text (D-02 wording): "Account connected, but Spotify Connect is not available yet — please retry from the SpotOn settings page." DE: "Konto verbunden, aber Spotify Connect ist noch nicht verfügbar — bitte über die SpotOn-Einstellungen erneut versuchen." Translate the remaining 9 languages in the same register as the neighboring PKCE strings.

    2. t/02_strings.t: add PLUGIN_SPOTON_CONNECT_DERIVE_FAILED to @bilingual_keys (Phase 50 Plan 03 convention).

    3. t/09_settings.t: Settings.pm now requires Plugins::SpotOn::API::Credentials and Plugins::SpotOn::Unified::DaemonManager inside _pkceStoreAccount — the stub harness must provide them or every _pkceStoreAccount test dies:
       - Add a Plugins::SpotOn::API::Credentials stub via write_stub: deriveCredentials($class, $accountId, $cb) invokes $cb->($next_derive_ok, $next_derive_ok ? undef : 'derivation_failed') where $next_derive_ok is a package-global (default 1), and records call count + last accountId into package globals the tests can read.
       - Add/extend a Plugins::SpotOn::Unified::DaemonManager stub with scheduleInit recording its call count.
       - New assertions: (a) a successful _pkceStoreAccount flow calls Credentials::deriveCredentials exactly once; (b) the same flow calls DaemonManager::scheduleInit at least once even when activeAccount was ALREADY set before the flow (Pitfall 6 regression guard — set the prefs stub's activeAccount to an existing account first); (c) with $next_derive_ok = 0, the flow still stores tokens/account (account creation not rolled back) and the rendered output contains the CONNECT_DERIVE_FAILED key (or, if output capture is impractical in the harness, a structure assertion that Settings.pm's failure branch references PLUGIN_SPOTON_CONNECT_DERIVE_FAILED).
       - Update the existing "structure tests" grep block: assert 'deriveCredentials' appears in Settings.pm.
  </action>
  <verify>
    <automated>prove t/02_strings.t && prove t/09_settings.t && prove t/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^PLUGIN_SPOTON_CONNECT_DERIVE_FAILED$' Plugins/SpotOn/strings.txt` returns 1, and the block contains 11 tab-indented language lines (CS DA DE EN ES FR IT NL NO PL SV each appear once within the block)
    - `grep -c 'PLUGIN_SPOTON_CONNECT_DERIVE_FAILED' t/02_strings.t` returns 1 (@bilingual_keys entry)
    - `prove t/02_strings.t` green (bilingual coverage including the new key)
    - `prove t/09_settings.t` green with the new deriveCredentials/scheduleInit assertions passing
    - `prove t/` full suite green
  </acceptance_criteria>
  <done>The D-02 warning string exists in all 11 languages with test coverage, and t/09_settings.t verifies the eager derivation call, the unconditional daemon-start trigger (Pitfall 6 guard), and the non-blocking failure behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → LMS Settings HTTP handlers | OAuth callback data enters _pkceStoreAccount |
| Settings.pm → Credentials.pm → spoton subprocess | fresh access token flows into derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-10 | Information Disclosure | derivation failure reason reflected into HTML result page | low | mitigate | Failure branch renders only the fixed i18n string, never the raw $reason (Phase 49 precedent: _renderPkceResultPage HTML-escapes, but raw reasons stay out of user-facing output entirely) (Task 1) |
| T-51-11 | DoS | repeated PKCE re-auth spamming derivations | low | mitigate | D-05 cooldown inside Credentials.pm applies to the eager path too (coalescing + rate limit are call-site-agnostic) |
| T-51-12 | Repudiation | silent derivation failure leaves user without Connect and without explanation | medium | mitigate | D-02 warning string rendered in the auth result page + WARN log with masked accountId (Task 1) |

No package-manager installs in this phase — no supply-chain row required.
</threat_model>

<verification>
- `prove t/02_strings.t`, `prove t/09_settings.t`, `prove t/` — all green
- Grep gates: single deriveCredentials call site, connectReady in both JSON branches, new i18n key present in 11 languages
- Manual (phase gate, with plan 02 landed): complete a PKCE auth in LMS Settings on the dev machine — credentials.json appears in the account dir within seconds, the success page shows, and the daemon starts without waiting for the 60s watchdog. Part of the `/gsd-verify-work` UAT alongside plan 02's Connect end-to-end check.
</verification>

<success_criteria>
- Every fresh PKCE authentication produces Connect-ready stored credentials in a single user-visible flow (AUTH-03 eager trigger)
- Success messaging is unified (fires post-derivation); failure messaging warns without blocking account creation (D-02)
- Daemon start is triggered unconditionally on derivation success (D-06), covering first-account, additional-account, and re-auth flows alike
</success_criteria>

<output>
Create `.planning/phases/51-credential-derivation-connect/51-03-SUMMARY.md` when done
</output>
