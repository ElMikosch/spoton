---
phase: 51-credential-derivation-connect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/API/Credentials.pm
  - t/16_credentials.t
  - t/05_perl_syntax.t
autonomous: true
requirements: [AUTH-03]
tags: [perl, oauth, pkce, credential-derivation, lms-plugin, librespot]

must_haves:
  truths:
    - "Credentials.pm converts a fresh PKCE access token into credentials.json (auth_type 1) via spoton --token-login without blocking the LMS event loop"
    - "Derivation success is verified by parsing credentials.json (auth_type==1, username, auth_data), never by subprocess stdout"
    - "Concurrent derivation requests for the same account coalesce into a single subprocess"
    - "Repeated derivation failures are rate-limited: 3 failures within 5 minutes triggers a 30-minute cooldown (D-05)"
    - "Account mismatch between credentials.json username and the PKCE account's spotifyUserId is detectable (D-08)"
  artifacts:
    - path: "Plugins/SpotOn/API/Credentials.pm"
      provides: "Shared derivation module (D-01): deriveCredentials, credentialsPathFor, verifyCredentials, accountMismatch, isCredentialError"
      exports: ["deriveCredentials", "credentialsPathFor", "verifyCredentials", "accountMismatch", "isCredentialError"]
    - path: "t/16_credentials.t"
      provides: "Unit coverage for every Credentials.pm behavior via stubbed Proc::Background/TokenManager/Helper"
  key_links:
    - from: "Plugins/SpotOn/API/Credentials.pm"
      to: "Plugins/SpotOn/API/TokenManager.pm"
      via: "TokenManager->getToken for a guaranteed-fresh access token (Pitfall 5 — never read pkce_tokens.json directly)"
      pattern: "TokenManager->getToken"
    - from: "Plugins/SpotOn/API/Credentials.pm"
      to: "spoton binary"
      via: "Proc::Background spawn of the token-login subcommand, timer-polled completion"
      pattern: "token-login"
---

<objective>
Create the shared credential-derivation module `Plugins::SpotOn::API::Credentials` (D-01: one module consumed by both the Settings-eager call site and the DaemonManager-lazy call site, wired in plans 02/03) plus its full unit test file `t/16_credentials.t`. The module converts a PKCE access token into long-lived librespot stored credentials (`credentials.json`, auth_type 1) by spawning `spoton --token-login` non-blockingly, and provides the supporting primitives the other two plans consume: account-mismatch detection (D-08), credential-error stderr classification (D-03), and re-derive rate limiting (D-05, Claude's discretion resolved as a counter+window+cooldown mirroring Daemon.pm's crash-loop constants).

Purpose: AUTH-03 — "PKCE access token is used once to obtain non-expiring librespot stored credentials." Every hard sub-problem (AP handshake, async subprocess polling, coalescing, rate limiting) already has a shipped analog in this codebase; this plan assembles them into one testable module.
Output: `Plugins/SpotOn/API/Credentials.pm`, `t/16_credentials.t` (Wave 0 test per Nyquist), `t/05_perl_syntax.t` updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/51-credential-derivation-connect/51-CONTEXT.md
@.planning/phases/51-credential-derivation-connect/51-RESEARCH.md
@.planning/phases/51-credential-derivation-connect/51-PATTERNS.md
@.claude/skills/spike-findings-spoton/references/credential-bridge.md
</context>

## Artifacts this phase produces

New symbols created by this plan (source-grounding exclusion list):

- **Module:** `Plugins::SpotOn::API::Credentials` (new file `Plugins/SpotOn/API/Credentials.pm`)
- **Public class methods:** `deriveCredentials($class, $accountId, $cb)`, `credentialsPathFor($class, $accountId)`, `verifyCredentials($class, $accountId)`, `accountMismatch($class, $accountId)`, `isCredentialError($class, $stderrText)`
- **Private subs:** `_pollDerivation`, `_accountDir`, `_mask`, `_recordFailure`, `_inCooldown`, `_resolveInflight`
- **Constants:** `CREDENTIALS_FILE` ('credentials.json'), `DERIVE_POLL_INTERVAL` (0.1), `DERIVE_POLL_MAX_ATTEMPTS` (100), `MAX_DERIVE_FAILURES` (3), `DERIVE_FAILURE_WINDOW` (300), `DERIVE_COOLDOWN_SECONDS` (1800)
- **Package-level state:** `%_deriveInflight` (accountId => arrayref of queued callbacks), `%_deriveFailures` (accountId => arrayref of failure epochs), `%_deriveCooldownUntil` (accountId => epoch)
- **Callback reason strings:** `no_token`, `no_binary`, `binary_too_old`, `spawn_failed`, `derivation_failed`, `rate_limited`
- **Test file:** `t/16_credentials.t`

<tasks>

<task type="auto">
  <name>Task 1: Write t/16_credentials.t — full behavioral test suite (RED)</name>
  <files>t/16_credentials.t</files>
  <read_first>
    - t/07_token_manager.t (the stub-harness convention to mirror: write_stub helper, tempdir stub_dir, unshift @INC, JSON::XS::VersionOneAndTwo stub delegating to JSON::PP — see its lines 1-200)
    - t/05_perl_syntax.t (Slim::Utils::Timers / Slim::Player stub examples)
    - .planning/phases/51-credential-derivation-connect/51-RESEARCH.md (Pattern 1 skeleton, credentials.json format, exact stderr error strings table)
    - Plugins/SpotOn/API/PKCE.pm (loadTokens/_accountDir shape at L307-370 — the read/parse convention verifyCredentials must match)
  </read_first>
  <behavior>
    - Test 1 (happy path): deriveCredentials with a stubbed TokenManager returning token 'tok-fresh', stubbed Helper (get => fake path, getCapability 'token-login' => 1), and a Proc::Background stub whose alive() returns false immediately; the test pre-writes a valid credentials.json ({"username":"userA","auth_type":1,"auth_data":"QUJD"}) into the account dir before the poll completes → cb receives (1, undef); credentials.json file mode is 0600 (skip the permission assertion on Windows via $^O check)
    - Test 2: credentials.json with auth_type 3 (token auth, not stored) → cb receives (0, 'derivation_failed')
    - Test 3: subprocess exits but no credentials.json exists → cb (0, 'derivation_failed'); corrupt/truncated JSON → cb (0, 'derivation_failed')
    - Test 4: TokenManager->getToken calls back with undef → cb (0, 'no_token') and Proc::Background->new was never called
    - Test 5: Helper getCapability('token-login') returns falsy → cb (0, 'binary_too_old') and no spawn
    - Test 6 (Pitfall 3 coalescing): two deriveCredentials calls for the same accountId before the first completes → exactly ONE Proc::Background->new spawn, BOTH callbacks resolved; a dying first callback does not starve the second (WR-06 eval-guard, mirror t/07's H3 test shape)
    - Test 7 (D-05 rate limit): after 3 consecutive failures within the window, a 4th call resolves (0, 'rate_limited') with no spawn; a subsequent success (simulate by clearing cooldown state or using a fresh accountId after expiring the window) resets the failure counter
    - Test 8 (D-08): accountMismatch returns 1 when credentials.json username is 'userB' but prefs accounts->{$accountId}{spotifyUserId} is 'userA'; returns 0 on match; returns 0 when credentials.json is absent or unparseable
    - Test 9 (D-03): isCredentialError matches each of the three exact librespot-core strings — 'Login failed with reason: Bad credentials', 'Login failed with reason: Could not validate credentials', and a line starting with 'No cached credentials in' — and does NOT match a generic panic line like 'thread main panicked at ...'
    - Test 10 (Pitfall 4): credentialsPathFor($accountId) ends with the accountId path segment followed by credentials.json (account-scoped, never the flat spoton dir)
    - Test 11 (invocation contract): the recorded Proc::Background spawn args contain, in order-independent presence: '--token-login', '--token', 'tok-fresh', '--cache', the account-scoped dir
    - Test 12 (T-29-07): no test-captured log line contains the full token value 'tok-fresh' (stub logger records messages into an array; assert none match the token)
  </behavior>
  <action>
    Create t/16_credentials.t mirroring t/07_token_manager.t's harness exactly: tempdir stub_dir + write_stub helper + unshift @INC, then `require Plugins::SpotOn::API::Credentials` from $project_dir.

    Stubs to write (per D-01 module contract below):
    - Log::Log4perl::Logger / Log::Log4perl / Slim::Utils::Log — same as t/07, but have the logger stub push formatted messages into a package-global @logged array so Test 12 can assert token absence
    - Slim::Utils::Prefs — preferences('server')->get('cachedir') returns a tempdir; preferences('plugin.spoton') backed by a %prefs_store hash exposing get/set (accounts, activeAccount)
    - Slim::Utils::Cache — hash-backed get/set/remove (needed transitively by TokenManager stub or Credentials if it caches nothing — keep minimal)
    - Slim::Utils::Timers — setTimer($obj, $when, $cb, @args) invokes $cb->($obj ? $obj : (), @args) SYNCHRONOUSLY (immediate dispatch) so the poll loop runs to completion inside the test; killTimers is a no-op. Guard against runaway recursion by having the Proc::Background stub report alive==0 from the first poll
    - Time::HiRes — time() delegating to CORE::time
    - Proc::Background — new(@args) records \@args into a package-global @spawns and returns a blessed stub; alive() returns a configurable package-global flag (default 0); die() no-op
    - Plugins::SpotOn::Helper — get() returns "$cache_dir/fake-spoton"; getCapability($key) reads a configurable package-global %caps (default: token-login => 1)
    - Plugins::SpotOn::API::TokenManager — getToken($class, $accountId, $cb) invokes $cb with a configurable package-global $next_token (default 'tok-fresh'); records call count
    - JSON::XS::VersionOneAndTwo — delegate from_json/to_json to JSON::PP (copy t/07's stub verbatim)

    Write all 12 behavior tests listed above. Where a test needs the module's internal state reset between cases (in-flight, failure counters), use distinct accountIds per test case instead of reaching into module internals.

    This is the RED step: the module does not exist yet, so `prove t/16_credentials.t` MUST fail at require time. Commit message prefix: test(51-01).
  </action>
  <verify>
    <automated>bash -c '! prove t/16_credentials.t >/dev/null 2>&1' (prove fails because Plugins/SpotOn/API/Credentials.pm does not exist yet)</automated>
  </verify>
  <acceptance_criteria>
    - t/16_credentials.t exists and `perl -c t/16_credentials.t` succeeds (the test file itself is syntactically valid)
    - `prove t/16_credentials.t` exits non-zero with a failure indicating Plugins::SpotOn::API::Credentials cannot be located
    - The file contains assertions for all 12 behaviors above (grep evidence: 'rate_limited', 'binary_too_old', 'accountMismatch', 'isCredentialError', 'no_token' each appear in the test file)
    - Timer stub dispatches callbacks synchronously (no sleep/usleep calls anywhere in the test file)
  </acceptance_criteria>
  <done>Complete failing test suite exists covering derivation success/failure verification, coalescing, rate limiting, mismatch detection, error-string classification, path scoping, invocation contract, and token-free logging.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Plugins/SpotOn/API/Credentials.pm (GREEN) + syntax-check registration</name>
  <files>Plugins/SpotOn/API/Credentials.pm, t/05_perl_syntax.t</files>
  <read_first>
    - t/16_credentials.t (the contract just written in Task 1 — implementation must satisfy it exactly)
    - Plugins/SpotOn/Unified/Daemon.pm (L248-364: Proc::Background spawn + _pollPortFile timer-poll shape to copy; L19-30: crash-loop constants naming convention)
    - Plugins/SpotOn/API/TokenManager.pm (L40, L254-343: %_refreshInflight coalescing with WR-06 eval-guarded drain — copy this exact shape; L467-471: _mask helper)
    - Plugins/SpotOn/API/PKCE.pm (L307-370: loadTokens read/parse eval-guard shape and _accountDir path construction to replicate)
    - .planning/phases/51-credential-derivation-connect/51-RESEARCH.md (Pattern 1 code skeleton, exact stderr strings, Assumption A2)
  </read_first>
  <action>
    Create Plugins/SpotOn/API/Credentials.pm implementing the contract from Task 1's tests. Structure (all names per "Artifacts this phase produces"):

    Header: strict/warnings; use File::Spec::Functions qw(catdir catfile); use JSON::XS::VersionOneAndTwo; use Slim::Utils::Log; use Slim::Utils::Prefs; use Slim::Utils::Timers; use Time::HiRes. Proc::Background is a lazy `require` inside deriveCredentials (Daemon.pm convention). Constants and package-level state hashes per the artifacts list.

    deriveCredentials($class, $accountId, $cb) — cb signature ($ok, $reason). Sequence (D-01 shared entry point):
    1. Coalescing guard first (Pitfall 3): if $_deriveInflight{$accountId} exists, push $cb onto the queue and return. Otherwise initialize the queue with [$cb]. Build a $resolve closure that deletes the queue and eval-guards each queued callback (a dying consumer must not starve remaining waiters — same WR-06 pattern as TokenManager::_refreshToken; log callback deaths at error level).
    2. Rate-limit gate (D-05): if _inCooldown($accountId), resolve (0, 'rate_limited') and warn (masked). Cooldown model: _recordFailure pushes time() onto $_deriveFailures{$accountId}, trims to the last MAX_DERIVE_FAILURES entries; when MAX_DERIVE_FAILURES failures fall within DERIVE_FAILURE_WINDOW seconds, set $_deriveCooldownUntil{$accountId} = time() + DERIVE_COOLDOWN_SECONDS and clear the failure list. Successful derivation clears both the failure list and any cooldown for that account.
    3. Binary gates: require Plugins::SpotOn::Helper; no helper path → resolve (0, 'no_binary'); Helper->getCapability('token-login') falsy → resolve (0, 'binary_too_old') with a WARN suggesting a SpotOn binary update (graceful degradation per RESEARCH Environment Availability).
    4. Fresh token (Pitfall 5): require Plugins::SpotOn::API::TokenManager; call TokenManager->getToken($accountId, sub {...}) — NEVER PKCE::loadTokens directly. Token undef → resolve (0, 'no_token') (TokenManager has already flagged needsReauth internally when permanent).
    5. Spawn: ensure the account dir exists (File::Path::make_path if missing, mirroring PKCE::storeTokens); eval { Proc::Background->new({ die_upon_destroy => 1 }, $helperPath, '-n', 'SpotOn', '--token-login', '--token', $token, '--cache', _accountDir($accountId)) }; spawn error → _recordFailure + resolve (0, 'spawn_failed'). CRITICAL (T-29-07/T-51-05): never log the spawn command line or the token value — log only the masked accountId and the cache dir.
    6. _pollDerivation via Slim::Utils::Timers::setTimer with a single state hashref arg { proc, accountId, resolve, attempts }: while proc->alive and attempts < DERIVE_POLL_MAX_ATTEMPTS, re-arm at DERIVE_POLL_INTERVAL (copy Daemon::_pollPortFile's poll-again-vs-complete branch structure). On timeout with proc still alive, call proc->die. On completion: success = verifyCredentials($accountId) returns a valid hashref; if success, chmod(0600, credentialsPathFor(...)) (the Rust binary controls the file's creation mode — enforce restrictive perms Perl-side per RESEARCH Security V3), clear failure state, resolve (1, undef); else _recordFailure + resolve (0, 'derivation_failed'). Success detection is FILE-BASED ONLY — do not read subprocess stdout (Windows stdout-piping precedent, commit 9874835).

    credentialsPathFor / _accountDir: account-scoped path exclusively — catdir(serverPrefs cachedir, 'spoton', $accountId) (Pitfall 4: new code must never touch the legacy flat spoton dir).

    verifyCredentials($class, $accountId): read credentialsPathFor, eval-guarded parse (PKCE::loadTokens shape), return the hashref only when auth_type == 1 AND username non-empty AND auth_data non-empty; undef otherwise (never trust exit code alone — RESEARCH Security V5).

    accountMismatch($class, $accountId) (D-08): returns 1 only when credentials.json parses, has a username, prefs accounts->{$accountId}{spotifyUserId} exists, and the two differ. All failure/absence cases return 0. This sub only DETECTS; deletion+re-derive is wired in plan 02. Deletion callers must unlink the single credentials.json file — pkce_tokens.json in the same dir must survive, so directory-tree removal (the TokenManager::removeAccount pattern) is explicitly the wrong tool here; note this in the sub's comment.

    isCredentialError($class, $stderrText) (D-03): return true iff text matches any of the three exact librespot-core substrings — 'Bad credentials', 'Could not validate credentials', 'No cached credentials in'. Add a comment citing Assumption A2: strings verified against the vendored librespot-core connection/mod.rs login_error_message(); recheck on any librespot-core dependency bump.

    _mask: substr($accountId, 0, 4) . '****' (identical to TokenManager convention); apply to every log line.

    Then add "$project_dir/Plugins/SpotOn/API/Credentials.pm" to the @pm_files list in t/05_perl_syntax.t (same convention as PKCE.pm's Phase 49 registration). If perl -c under the syntax harness needs additional stubs for Credentials.pm's imports, extend t/05's stub set following its existing patterns.
  </action>
  <verify>
    <automated>prove t/16_credentials.t && prove t/05_perl_syntax.t && prove t/</automated>
  </verify>
  <acceptance_criteria>
    - `prove t/16_credentials.t` passes (all Task 1 behaviors green)
    - `prove t/` full suite green (no regressions across all existing test files)
    - t/05_perl_syntax.t @pm_files contains Plugins/SpotOn/API/Credentials.pm
    - Comment-filtered source contains no blocking subprocess idioms: `grep -v '^\s*#' Plugins/SpotOn/API/Credentials.pm | grep -cE 'qx|\bsystem\s*\(' ` returns 0 (Pitfall 2)
    - `grep -v '^\s*#' Plugins/SpotOn/API/Credentials.pm | grep -c 'loadTokens'` returns 0 — token freshness comes from TokenManager->getToken only (Pitfall 5)
    - `grep -c 'credentials_saved' Plugins/SpotOn/API/Credentials.pm` returns 0 — success detection is file-based, not stdout-based
    - `grep -v '^\s*#' Plugins/SpotOn/API/Credentials.pm | grep -c 'remove_tree'` returns 0 — the module never deletes directory trees
  </acceptance_criteria>
  <done>Credentials.pm implements the full derivation contract, all 12 test behaviors pass, syntax check covers the new module, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LMS Perl → spoton subprocess | access token crosses process boundary via argv |
| spoton subprocess → Spotify AP | AP handshake returns reusable_auth_credentials |
| credentials.json on disk | long-lived Connect session credential readable by local users |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | Credentials::verifyCredentials | medium | mitigate | Validate auth_type==1 + non-empty username/auth_data after every derivation; never trust exit code alone (Task 2) |
| T-51-03 | Information Disclosure | credentials.json file perms | high | mitigate | chmod 0600 after successful derivation; account dir already 0700 (Settings.pm convention) (Task 2) |
| T-51-04 | DoS / Tampering | concurrent --token-login spawns | medium | mitigate | %_deriveInflight coalescing (Pitfall 3); librespot-core atomic write-then-rename as defense-in-depth (Task 2) |
| T-51-05 | Information Disclosure | access token in subprocess argv (/proc cmdline) | low | accept | Binary contract requires --token argv (no env alternative without Rust rebuild — out of Phase 51 scope). Accepted: subprocess lives <1s, token TTL 1h, single-user LMS host. Compensating control: token never logged (T-29-07 discipline, Test 12) |
| T-51-06 | DoS | derivation retry loop hammering Spotify AP | medium | mitigate | D-05 rate limiter: 3 failures / 5 min → 30 min cooldown (Task 2) |

No package-manager installs in this phase (RESEARCH: Package Legitimacy Audit N/A) — no supply-chain row required.
</threat_model>

<verification>
- `prove t/16_credentials.t` — all module behaviors
- `prove t/` — full suite green (503+ existing tests plus new file)
- No blocking subprocess calls, no stdout-based success detection, no flat-dir path usage (grep gates in Task 2)
</verification>

<success_criteria>
- Plugins::SpotOn::API::Credentials exists with the five public methods and passes all 12 behavioral tests
- AUTH-03's core mechanism (PKCE token → credentials.json auth_type 1) is implemented and unit-verified with a stubbed subprocess
- D-05 rate limiting and D-08 mismatch detection primitives are ready for plan 02/03 wiring
</success_criteria>

<output>
Create `.planning/phases/51-credential-derivation-connect/51-01-SUMMARY.md` when done
</output>
