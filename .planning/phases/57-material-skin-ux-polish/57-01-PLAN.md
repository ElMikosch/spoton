---
phase: 57-material-skin-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/toptracks.png
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png
autonomous: true
requirements: ["GH-124"]

must_haves:
  truths:
    - "Every item pushed by _homeFeed (Recently Played, Made For You expired branch, Made For You normal branch, Top Tracks) carries an image key, so Material Skin's grid/cover toggle works on the Home feed (CONTEXT decision A+E)"
    - "The 3 new PNG assets match the existing SpotOn/Spotty icon style: 512x512, 8-bit gray+alpha, white glyph on transparent background"
    - "Non-Material skins (Default skin) render the PNGs directly as menu icons"
  artifacts:
    - "Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/toptracks.png"
    - "Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png"
    - "Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png"
    - "Plugins/SpotOn/Plugin.pm with image keys on all 4 _homeFeed push sites"
  key_links:
    - "OPML image path 'plugins/SpotOn/html/images/{name}.png' resolves to Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/{name}.png via the LMS HTML root convention (same mechanism Phase 56 proved with home.png/podcasts.png)"
    - "Made For You has TWO push sites in _homeFeed (expired branch ~line 1274 and normal branch ~line 1280) — both must get the same madeforyou.png key or the toggle breaks whenever sp_dc expires"
---

<objective>
Add icons to all Home feed items (GH #124 follow-up, CONTEXT decision A+E). Material Skin disables the grid/cover-view toggle when ANY item in a list lacks an icon — Phase 56 fixed this for playlists/albums/etc., but the Home feed itself (`_homeFeed`) still pushes icon-less items.

Purpose: Restore the grid/cover toggle on the SpotOn Home feed in Material Skin and give the Home feed proper icons in all skins.

Output: 3 new PNG assets (toptracks.png, recently.png, madeforyou.png) and image keys on all 4 `_homeFeed` push sites in Plugin.pm.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-material-skin-ux-polish/57-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — toptracks.png asset + image key on the Top Tracks entry</name>
  <files>
    Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/toptracks.png,
    Plugins/SpotOn/Plugin.pm
  </files>
  <read_first>
    Plugins/SpotOn/Plugin.pm lines 1250-1294 (_homeFeed — the 4 push sites),
    .planning/phases/57-material-skin-ux-polish/57-CONTEXT.md (decision A+E)
  </read_first>
  <action>
    Thinnest end-to-end slice proving the asset-plus-image-key path for the Home feed, per CONTEXT decision A+E.

    **Asset:** Copy Spotty's existing top-tracks icon into SpotOn's image directory (same precedent as Phase 56, which copied home.png/podcasts.png/song.png/account.png from the same source):
    - Source: /home/sti/spotty-ng/Spotty-Plugin/HTML/EN/plugins/Spotty/html/images/toptracks.png
    - Destination: Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/toptracks.png
    - The source is 512x512, 8-bit gray+alpha — the exact style all existing SpotOn menu icons use.

    **Plugin.pm:** In `_homeFeed` (around line 1287), add an image key to the Top Tracks push:
    `image => 'plugins/SpotOn/html/images/toptracks.png'` — same path convention Phase 56 established in handleFeed (e.g. the Podcasts entry uses 'plugins/SpotOn/html/images/podcasts.png').

    Do not touch the other 3 push sites yet — that is Task 2's expansion.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/Plugin.pm 2>&1 && echo "---asset---" && file Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/toptracks.png | grep "512 x 512" && echo "---image-key---" && grep -c "plugins/SpotOn/html/images/toptracks.png" Plugins/SpotOn/Plugin.pm</automated>
  </verify>
  <done>toptracks.png exists (512x512 gray+alpha), the Top Tracks entry in _homeFeed carries its image key, perl -c passes. The asset-path-to-OPML-image-key pipeline for the Home feed is proven end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Generate recently.png + madeforyou.png, wire remaining 3 image keys</name>
  <files>
    Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png,
    Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png,
    Plugins/SpotOn/Plugin.pm
  </files>
  <read_first>
    Plugins/SpotOn/Plugin.pm lines 1250-1294 (_homeFeed after Task 1)
  </read_first>
  <action>
    **Assets (CONTEXT decision A+E: "Use Spotty's PNGs as style reference — same dimensions, similar visual language"):** Spotty has no recently-played or made-for-you icon, so generate the 2 PNGs with a throwaway Python/PIL script (PIL 10.2.0 is installed; write the script to the session scratchpad directory, do NOT commit it — only the PNGs are deliverables).

    Style contract (matches `file` output of every existing icon in the directory): 512x512 px, grayscale+alpha PNG (PIL mode "LA"), fully transparent background, white (255) glyph. Render at 4x (2048px) with ImageDraw and downscale to 512 with LANCZOS for antialiasing. Keep the glyph within a centered ~420px bounding box like the Spotty icons.

    - recently.png — clock glyph: circle outline (stroke ~40px at final scale, radius ~190px, centered) with two hands from center (minute hand up to 12 o'clock, hour hand toward ~4 o'clock, stroke ~40px, rounded ends via circles at the tips).
    - madeforyou.png — sparkle glyph (Spotify's Made-For-You iconography): one large four-pointed concave star centered slightly low-left (tip-to-tip ~340px; build as a polygon with 4 outer tips and 4 inner concave points at ~22% of the tip radius) plus one small four-pointed star (~120px) in the upper-right area.

    After generating, verify each renders as a recognizable white glyph (open with PIL, assert >1% and <40% of pixels have alpha > 0 — guards against blank or solid images).

    **Plugin.pm — remaining 3 push sites in `_homeFeed`:**
    - Recently Played entry (~line 1255): add `image => 'plugins/SpotOn/html/images/recently.png'`
    - Made For You expired branch (~line 1274): add `image => 'plugins/SpotOn/html/images/madeforyou.png'`
    - Made For You normal branch (~line 1280): add `image => 'plugins/SpotOn/html/images/madeforyou.png'`

    Both Made For You branches MUST get the key — CONTEXT decision A+E calls out all push sites explicitly; missing the expired branch would silently break the grid toggle only for users with an expired sp_dc.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/Plugin.pm 2>&1 && echo "---assets---" && file Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png | grep -c "512 x 512" && echo "---keys---" && grep -c "plugins/SpotOn/html/images/recently.png" Plugins/SpotOn/Plugin.pm && grep -c "plugins/SpotOn/html/images/madeforyou.png" Plugins/SpotOn/Plugin.pm && python3 -c "from PIL import Image; import sys; [sys.exit(1) for p in ['Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png','Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png'] if not (0.01 < (lambda im: sum(1 for a in im.convert('RGBA').getchannel('A').getdata() if a>0)/(im.width*im.height))(Image.open(p)) < 0.40)]; print('glyph-ok')"</automated>
  </verify>
  <done>recently.png and madeforyou.png exist (512x512, white glyph on transparent, 1-40% coverage), madeforyou.png key appears at exactly 2 push sites (grep count = 2), recently.png key at 1, perl -c passes. All 4 _homeFeed items now carry image keys (decision A+E complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Static image paths in OPML output — no user-controlled input. Asset files are generated locally or copied from Spotty (same-license LMS plugin, Phase 56 precedent). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-01 | Information Disclosure | Static icon paths in _homeFeed | low | accept | Paths reference SpotOn-bundled PNGs only; identical pattern to Phase 56 menu icons. |
</threat_model>

<verification>
1. `perl -c Plugins/SpotOn/Plugin.pm` passes
2. 3 PNG assets exist, all 512x512 grayscale+alpha
3. grep: toptracks.png key x1, recently.png key x1, madeforyou.png key x2 in Plugin.pm
4. Manual (post-execution, local LMS): Material Skin Home feed shows grid/cover toggle; Default skin shows the 4 icons
</verification>

<success_criteria>
- GH #124 follow-up resolved: all 4 _homeFeed items carry image keys, Material Skin grid/cover toggle works on the Home feed
- 3 new icon assets match the established 512x512 gray+alpha style
- No regression: perl -c passes, no other _homeFeed behavior changed
</success_criteria>

<output>
Create `.planning/phases/57-material-skin-ux-polish/57-01-SUMMARY.md` when done
</output>
