---
phase: 57-material-skin-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Status.pm
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HomeExtras.pm
autonomous: true
requirements: ["GH-125"]

must_haves:
  truths:
    - "JSON responses from the Status and Settings AJAX endpoints carry single-encoded UTF-8 — umlauts (e.g. German account names) render correctly instead of mojibake (CONTEXT decision B)"
    - "Material Skin home screen offers Made For You as a scrolled row, registered unconditionally like Spotty's rows (CONTEXT decision D)"
    - "When sp_dc is missing/expired, the Made For You row degrades cleanly: _madeForYouFeed returns textarea hint items which the existing WR-01 filter in HomeExtraBase strips, leaving an empty row instead of junk cards"
  artifacts:
    - "Plugins/SpotOn/Status.pm with direct to_json assignment in _jsonResponse and no Encode import"
    - "Plugins/SpotOn/Settings.pm with direct to_json assignment in _jsonResponse (Encode import retained for the HTML/diag-text sites)"
    - "Plugins/SpotOn/HomeExtras.pm with a Plugins::SpotOn::HomeExtraMadeForYou package registered in the top-level initializer"
  key_links:
    - "to_json from JSON::XS::VersionOneAndTwo already returns UTF-8 octets — wrapping it in a second encode pass is what produced the double-encoding (decision B root cause)"
    - "HomeExtraMadeForYou subclasses Plugins::SpotOn::HomeExtraBase, so it inherits the WR-01 textarea filter and WR-02 60s memoization automatically — no per-extra gating code needed (decision D)"
    - "Registration flows through the existing postinitPlugin require of Plugins::SpotOn::HomeExtras (Plugin.pm:300-309) — Plugin.pm needs NO change"
---

<objective>
Fix the UTF-8 double-encoding in the Status/Settings JSON helpers (GH #125, CONTEXT decision B) and add Made For You as a Material Skin scrolled row (CONTEXT decision D).

Purpose: Umlauts in AJAX JSON responses currently arrive double-encoded (mojibake). Made For You is the only Home feed section missing from the MS scrolled rows added in Phase 56.

Output: Corrected `_jsonResponse` in Status.pm and Settings.pm; new `HomeExtraMadeForYou` package in HomeExtras.pm.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-material-skin-ux-polish/57-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove double-encoding from _jsonResponse in Status.pm and Settings.pm</name>
  <files>
    Plugins/SpotOn/Status.pm,
    Plugins/SpotOn/Settings.pm
  </files>
  <read_first>
    Plugins/SpotOn/Status.pm lines 1-15 and 314-329 (Encode import + _jsonResponse),
    Plugins/SpotOn/Settings.pm lines 1-20 and 1000-1010 (Encode import + _jsonResponse),
    .planning/phases/57-material-skin-ux-polish/57-CONTEXT.md (decision B — scope boundary)
  </read_first>
  <action>
    Per CONTEXT decision B: `to_json` (from JSON::XS::VersionOneAndTwo, imported in both files) already returns UTF-8 octets, so the additional Encode pass in `_jsonResponse` double-encodes multi-byte characters.

    **Status.pm (line ~321):** In `_jsonResponse`, replace the `$bytes` assignment so it assigns the `to_json($data)` result directly, with no Encode wrapper: `my $bytes = to_json($data);`. Then delete the now-unused Encode import statement at line ~6 entirely — line 321 was its only call site in this file.

    **Settings.pm (line ~1003):** Apply the identical `$bytes` assignment change in its `_jsonResponse`. Do NOT delete the Encode import in Settings.pm — lines ~820 (HTML page body) and ~960 (diag text download) legitimately encode Perl character strings and are explicitly OUT of scope per decision B.

    Do not add any comment mentioning the removed wrapper expression — the verify gates below negative-grep for it and a commemorative comment would re-trip them.

    Note `Content-Length => length($bytes)` stays correct: length() on the octet string returned by to_json counts bytes.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/Status.pm 2>&1 && perl -c Plugins/SpotOn/Settings.pm 2>&1 && echo "---no-double-encode---" && test "$(grep -v '^\s*#' Plugins/SpotOn/Status.pm | grep -c "encode('UTF-8', to_json")" -eq 0 && test "$(grep -v '^\s*#' Plugins/SpotOn/Settings.pm | grep -c "encode('UTF-8', to_json")" -eq 0 && echo "---direct-assign---" && grep -cF 'my $bytes = to_json($data);' Plugins/SpotOn/Status.pm && grep -cF 'my $bytes = to_json($data);' Plugins/SpotOn/Settings.pm && echo "---encode-imports---" && test "$(grep -v '^\s*#' Plugins/SpotOn/Status.pm | grep -c 'use Encode')" -eq 0 && test "$(grep -v '^\s*#' Plugins/SpotOn/Settings.pm | grep -c "encode('UTF-8'")" -eq 2 && echo OK</automated>
  </verify>
  <done>Both _jsonResponse subs assign to_json output directly. Status.pm has no Encode import left; Settings.pm keeps exactly its 2 remaining out-of-scope encode sites (~820, ~960). perl -c passes for both files.</done>
</task>

<task type="auto">
  <name>Task 2: Add HomeExtraMadeForYou scrolled row to HomeExtras.pm</name>
  <files>Plugins/SpotOn/HomeExtras.pm</files>
  <read_first>
    Plugins/SpotOn/HomeExtras.pm (full file — the 4-package layout and the WR-01/WR-02 feed wrapper in HomeExtraBase),
    Plugins/SpotOn/Plugin.pm lines 1394-1420 (_madeForYouFeed — signature and textarea degradation behavior),
    .planning/phases/57-material-skin-ux-polish/57-CONTEXT.md (decision D)
  </read_first>
  <action>
    Per CONTEXT decision D, add Made For You as a third scrolled row, registered unconditionally (same as Spotty):

    **New package** `Plugins::SpotOn::HomeExtraMadeForYou` appended to HomeExtras.pm, following the exact shape of the existing HomeExtraRecentlyPlayed/HomeExtraTopTracks packages: `use strict; use warnings; use base qw(Plugins::SpotOn::HomeExtraBase);` and an initPlugin that calls SUPER::initPlugin with:
    - title => 'PLUGIN_SPOTON_MADE_FOR_YOU' (already exists in strings.txt:651 with all 11 languages — NO i18n changes needed)
    - subtitle => 'PLUGIN_SPOTON'
    - feed => \&Plugins::SpotOn::Plugin::_madeForYouFeed
    - tag => 'MadeForYou'

    End the package with `1;`.

    **Registration:** In the top-level `Plugins::SpotOn::HomeExtras` package (top of file), add `Plugins::SpotOn::HomeExtraMadeForYou->initPlugin();` after the existing two initPlugin calls. No Plugin.pm change — postinitPlugin (Plugin.pm:300-309) already requires this file, which self-registers at load.

    **Why unconditional registration is safe (decision D rationale — do not add gating):** `_madeForYouFeed` matches the ($client, $callback, $args) signature HomeExtraBase expects, and on missing/expired sp_dc or Pathfinder failure it returns only `type => 'textarea'` hint items — the WR-01 filter in the inherited HomeExtraBase feed wrapper strips those, so the row renders empty instead of showing junk cards. The Phase 56 decision to skip Made For You predated this filter analysis; decision D supersedes it.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && grep -c '^package Plugins::SpotOn::HomeExtraMadeForYou;' Plugins/SpotOn/HomeExtras.pm && grep -c 'Plugins::SpotOn::HomeExtraMadeForYou->initPlugin' Plugins/SpotOn/HomeExtras.pm && grep -c "tag      => 'MadeForYou'" Plugins/SpotOn/HomeExtras.pm && grep -c 'Plugins::SpotOn::Plugin::_madeForYouFeed' Plugins/SpotOn/HomeExtras.pm && grep -c "title    => 'PLUGIN_SPOTON_MADE_FOR_YOU'" Plugins/SpotOn/HomeExtras.pm</automated>
  </verify>
  <done>HomeExtras.pm contains the HomeExtraMadeForYou package (base SpotOn::HomeExtraBase, feed _madeForYouFeed, tag MadeForYou, existing i18n title) and the top-level initializer registers it. NOTE: perl -c is NOT possible for HomeExtras.pm outside LMS runtime (MaterialSkin::HomeExtraBase dependency + load-time initPlugin calls) — structural grep gates above plus LMS restart without "Could not load SpotOn Home Extras" in server.log are the verification, same as Phase 56.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LMS web UI → _jsonResponse | Encoding-only change on the response path; no change to what data is exposed or to auth/CSRF handling of the endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-02 | Tampering | _jsonResponse output | low | accept | Change removes a redundant encoding pass only; JSON structure, Content-Length correctness, and headers unchanged. |
| T-57-03 | Information Disclosure | Made For You HomeExtra feed | low | accept | _madeForYouFeed already enforces per-account gating via _getAccountId + WebPlayer state; failure paths render fixed i18n hints (which WR-01 strips), never raw errors — same posture as the two existing extras (T-56-02). |
</threat_model>

<verification>
1. `perl -c` passes for Status.pm and Settings.pm
2. Negative grep: no double-encode wrapper remains around to_json in either _jsonResponse
3. Settings.pm retains exactly 2 encode sites (~820, ~960)
4. HomeExtras.pm structural greps pass (package, registration, feed ref, tag, title)
5. Manual (post-execution, local LMS with Material Skin): umlaut-bearing JSON fields (e.g. German strings on the Status page) render correctly; MS home screen customization offers "Made For You" row; row shows Pathfinder playlists with artwork when sp_dc is valid
</verification>

<success_criteria>
- GH #125 encoding portion resolved: single-encoded UTF-8 from both _jsonResponse helpers (decision B), out-of-scope encode sites untouched
- GH #125 Made For You portion resolved: third scrolled row registered unconditionally with clean sp_dc degradation (decision D)
- No i18n changes needed (existing PLUGIN_SPOTON_MADE_FOR_YOU key covers all 11 languages)
</success_criteria>

<output>
Create `.planning/phases/57-material-skin-ux-polish/57-02-SUMMARY.md` when done
</output>
