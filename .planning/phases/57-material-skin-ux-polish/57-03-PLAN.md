---
phase: 57-material-skin-ux-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/57-material-skin-ux-polish/upstream-ms-issue-draft.md
autonomous: false
requirements: ["GH-125"]

must_haves:
  truths:
    - "An upstream issue exists at CDrummond/lms-material describing the getHomeExtra3rdPartyItems double-encoding with a reproduction and a fix suggestion (CONTEXT decision C)"
    - "The issue text was reviewed and approved by the developer before posting — external posts are one-way and never auto-approved"
    - "The issue text is in English (project convention for code and community communication)"
  artifacts:
    - ".planning/phases/57-material-skin-ux-polish/upstream-ms-issue-draft.md (approved draft, kept as record)"
    - "Issue URL recorded in the plan SUMMARY"
  key_links:
    - "MaterialSkin Plugin.pm sub getHomeExtra3rdPartyItems (~line 556) returns to_json(...) UTF-8 octets, and ~line 2049 passes that string into $request->addResult('items', ...) where the outer JSON-RPC/CometD serializer re-encodes it — that second pass is the mojibake source"
    - "No SpotOn-side workaround exists (decision C): the double-encoding happens entirely inside Material Skin after SpotOn's strings leave Slim::Utils::Strings; Spotty exhibits the identical bug"
---

<objective>
File the upstream Material Skin encoding bug (CONTEXT decision C): `getHomeExtra3rdPartyItems()` double-encodes UTF-8, so non-ASCII HomeExtra titles/subtitles (e.g. German "Für dich gemacht") show as mojibake on the MS home screen. Confirmed upstream — affects Spotty identically, no SpotOn-side workaround possible.

Purpose: The only path to fixing umlaut mojibake in scrolled-row titles is an upstream fix at CDrummond/lms-material.

Output: Reviewed issue draft (kept in the phase directory) and a filed GitHub issue with its URL recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-material-skin-ux-polish/57-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the upstream issue (English) with repro + fix suggestion</name>
  <files>.planning/phases/57-material-skin-ux-polish/upstream-ms-issue-draft.md</files>
  <read_first>
    /var/lib/squeezeboxserver/cache/InstalledPlugins/Plugins/MaterialSkin/Plugin.pm lines 548-570 (getHomeExtra3rdPartyItems) and lines 2040-2055 (the addResult call site),
    /var/lib/squeezeboxserver/cache/InstalledPlugins/Plugins/MaterialSkin/install.xml (installed MS version for the issue's environment section),
    .planning/phases/57-material-skin-ux-polish/57-CONTEXT.md (decision C)
  </read_first>
  <action>
    Write the complete issue text (English — project convention) to the draft file. Before drafting, check whether the bug still exists on Material Skin's current master (fetch https://raw.githubusercontent.com/CDrummond/lms-material/master/MaterialSkin/Plugin.pm and locate getHomeExtra3rdPartyItems) — cite master line numbers if it does; if it was already fixed upstream, STOP and report to the developer instead of drafting.

    Draft structure:
    - Title: e.g. "Non-ASCII 3rd-party Home Extra titles are double-encoded (mojibake) in getHomeExtra3rdPartyItems"
    - Environment: installed MS version (from install.xml), LMS version, confirmation the code is unchanged on master
    - Symptom: a 3rd-party plugin registering a Home Extra whose translated title contains non-ASCII (e.g. German "Für dich gemacht") renders as mojibake ("FÃ¼r dich gemacht") on the MS home screen; reproducible with both Spotty and SpotOn on a German-language LMS
    - Root cause: getHomeExtra3rdPartyItems returns the output of to_json, which produces UTF-8 octets. The caller passes that byte string into the addResult("items", ...) call, and the outer JSON-RPC/CometD response serialization encodes it again — UTF-8 bytes get re-encoded as if they were Latin-1 characters. Cite both code sites with file + line references.
    - Fix suggestion: decode the octets back to a Perl character string before handing them to addResult (Encode::decode of the to_json output), or return the plain Perl data structure and serialize once at the boundary. Phrase as a suggestion, not a demand; note that titles pass through Slim::Utils::Strings::getString, which returns character data that to_json then encodes.
    - Tone: factual and helpful; where the diagnosis relies on our code reading rather than an MS-side test, mark it clearly as our analysis (hedge hypotheses, do not overstate).

    Do NOT post anything in this task — draft only.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && test -s .planning/phases/57-material-skin-ux-polish/upstream-ms-issue-draft.md && grep -c "getHomeExtra3rdPartyItems" .planning/phases/57-material-skin-ux-polish/upstream-ms-issue-draft.md && grep -c "addResult" .planning/phases/57-material-skin-ux-polish/upstream-ms-issue-draft.md</automated>
  </verify>
  <done>Draft file exists in the phase directory, in English, containing symptom, environment, root-cause analysis with both MS code references, and a hedged fix suggestion. Nothing posted yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Checkpoint — developer approves the upstream issue draft before posting</name>
  <what-built>Complete English issue draft for CDrummond/lms-material at .planning/phases/57-material-skin-ux-polish/upstream-ms-issue-draft.md, covering the getHomeExtra3rdPartyItems double-encoding (repro, root cause, fix suggestion).</what-built>
  <how-to-verify>
    Read the draft file and check:
    1. Technical accuracy of the root-cause analysis (double-encoding at the two cited MS code sites)
    2. Tone — factual, helpful, hypotheses clearly hedged
    3. Nothing you would not want posted publicly under your name
    This checkpoint is NEVER auto-approvable (project rule: external issues/posts only after explicit approval), regardless of yolo/auto_advance mode.
  </how-to-verify>
  <resume-signal>Type "approved" to post as-is, or describe edits to apply before posting</resume-signal>
</task>

<task type="auto">
  <name>Task 3: File the issue at CDrummond/lms-material</name>
  <files>.planning/phases/57-material-skin-ux-polish/upstream-ms-issue-draft.md</files>
  <precondition>`gh auth status` succeeds for github.com (posting requires an authenticated gh CLI)</precondition>
  <action>
    Only after explicit human approval from the preceding checkpoint (posting to an external repo is one-way — it cannot be unsent).

    Extract the title line from the approved draft, then post the body via gh:
    `gh issue create -R CDrummond/lms-material --title "<title from draft>" --body-file <body extracted from draft>`
    (Strip the title heading from the body file before posting so it is not duplicated.)

    Capture the returned issue URL. Append it to the bottom of upstream-ms-issue-draft.md as a "Filed as: <URL> (date)" footer line, and record it in the plan SUMMARY. Do not modify any labels/assignees on the upstream repo.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && grep -c "Filed as: https://github.com/CDrummond/lms-material/issues/" .planning/phases/57-material-skin-ux-polish/upstream-ms-issue-draft.md</automated>
  </verify>
  <done>Issue is live at CDrummond/lms-material, its URL is recorded in the draft file footer and the SUMMARY. CONTEXT decision C complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local draft → public GitHub issue | One-way public disclosure boundary; gated by a blocking human checkpoint before crossing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-04 | Information Disclosure | Public issue text | low | mitigate | Human review checkpoint before posting; draft contains only MS code analysis and generic repro — no SpotOn credentials, tokens, account names, or private paths. |
| T-57-05 | Repudiation | External post under developer's account | low | mitigate | Checkpoint is blocking and never auto-approved; approved draft retained in phase directory as the record of what was posted. |
</threat_model>

<verification>
1. Draft exists, English, technically grounded in the two MS code sites
2. Human checkpoint approval recorded before any posting
3. Issue URL present in draft footer and SUMMARY
</verification>

<success_criteria>
- CONTEXT decision C complete: upstream issue filed at CDrummond/lms-material with repro + fix suggestion
- No external post happened without explicit developer approval
- If the bug turns out to be already fixed on MS master, the plan stops at Task 1 with a report instead — that outcome also satisfies the phase goal (upstream disposition clarified)
</success_criteria>

<output>
Create `.planning/phases/57-material-skin-ux-polish/57-03-SUMMARY.md` when done
</output>
